# Overnight log

- 2026-09-16 round 1, expired quote (plain price ask vs haggling): the live door refused to open (no COMMS_V2_DATABASE_URL, no .envrc in this worktree), so the round was driven through the repo desk/quoting suites (79 pass). Haggling holds for Ben with the money fixed line and no reissue, as briefed. A plain price ask holds as stale_quote with no +5% reissue; that reissue is not recorded in behaviour.md and contracts.md defers it, so no fix was made: OVERNIGHT-QUESTIONS.md items 1 and 2.
- 2026-09-16 round 2, opt-out and STOP on WhatsApp: the live door still has no environment, so the round ran through the desk suite. After a tap enquiry, a bare "STOP" went to the router and composer and the customer got "Thanks, leave it with me and I'll come back to you." plus a hold; a first-message "Please do not contact me again" was answered too. Fix: the desk now gives an opt-out turn (server/opt-out.ts `detectOptOut`, moved to the database-free server/opt-out-detect.ts) no reply and no model call, as the old desk's gate 0 does; regression test in desk.test.ts.
- 2026-09-16 round 3, bubble size on a mixed coverage-plus-job WhatsApp reply: the live door still has no environment, so the round ran the composer-shaped replies through the desk's own renderer and suite. "Yes we cover SE15 ... let me know roughly how many there are, e.g. two or three? Thanks." went out as two bubbles split mid-phrase, the first ending "e.g." and the second starting "two or three?" (answer 93: never mid-phrase). Fix: the WhatsApp splitter (server/comms-v2/desk/sender.ts `sentencesIn`) no longer ends a sentence at a title (Mr./Mrs./Ms./Dr.) or at a short form (e.g., i.e., approx., etc., no., St.) that a lower-case word or number follows; regression test in sender.test.ts.
- 2026-09-16 round 4, phone calls (missed, then answered, with transcript and summary on the board's bubble): the live door still has no environment, so the round ran the call door through the channel-door suite and read the resulting file through the board's `callViewOf`. A call driven through the door could never carry the call's summary (the door took no summary, though the live path records the telephony side's `jobSummary`), so every sandbox call bubble read "No summary yet"; and a missed call's bubble said "No summary yet" and "No transcript yet" although a missed call never gets a transcript. Fix: the call door takes an optional `summary` (server/comms-v2/channels/call-adapter.ts `validateDoorCall`/`fromDoorCall`), and the board's call bubble (CommsV2BoardPage.tsx `CallTurnBody`) promises neither on a missed call; regression checks in channel-doors.test.ts and CommsV2BoardPage.test.tsx.
- 2026-09-16 round 5, SMS with a photo (MMS) after the desk asked for one: the live door still has no environment, so the round ran the Twilio SMS shape through the channel gateway and desk suite (and realistic replies through `renderSms`, which rendered them correctly). The customer's texted photo, which a UK long code cannot receive, reached the router as "customer: [empty]" and the composer as an empty line: the adapter's recorded media failure was dropped at the gateway, so the reply could only ignore the photo the customer thinks they sent (the SMS adapter's own contract says "never silent"). Fix: the turn now carries `mediaFailed` (server/comms-v2/desk/case-file.ts, set by both gateways), and the router's and composer's threads show "[1 photo or video sent that did not reach us]"; regression test in channel-desk.test.ts. What the reply should then ask (resend on WhatsApp, or re-ask for a photo the ledger already marks asked) is left to the composer; no rule was added.
- 2026-09-16 round 6, a plain thanks on WhatsApp after the quote was promised: the live door still has no environment, so the round ran through the desk's Kiran case-study suite with a composer that rewords the promise. After "That's everything I need for now. I'll put the quote together and send it over to you here.", a "thanks mate" got "No problem, Sam. I'll send you the quote on here." back, which repeats the promise in new words (answer 90). The repeat check (server/comms-v2/desk/repeat.ts `isWrapUp`) only knew "put together", "send ... over", "price up", "working on", "sort out" and "get ... over". It missed "I'll send you the quote", "Your quote will be with you shortly", "The quote's on its way", "Quote to follow", "I'll come back to you with a quote", "You'll have the quote", "pop it across" and "being prepared". (A "later today" wording is already held by the date/time guard.) Fix: the check now reads those wordings, and "price" and "pricing" as well as "quote", as the same wrap-up, and never counts a question as one; regression tests in kiran-case-study.test.ts (a wording table, and a desk-level thanks that now gets only "No worries at all, Sam.").
- 2026-09-16 round 7, web enquiry form answered by SMS (number not on WhatsApp, job and NG9 postcode on the form): the live door still has no environment, so the round ran the form through the channel gateway and desk with a scripted composer, printing each brief. The first SMS brief rightly asked for one access question (job and location already on the form), but a reply whose only question was "Could you send a photo of the tap?" marked access asked as well as photos: the desk spent the proposed subject on any "?" in the reply. The next brief then said "Never ask again: photos or video, access" and wrapped up, so access was never asked (a call-offer question such as "is it OK if I give you a quick call?" spent it the same way). Fix: the proposed subject is spent only by a question about the job or that subject (server/comms-v2/desk/lexicon.ts `asksProposed`, used in desk.ts `afterSend`); regression test in channel-desk.test.ts, where the second SMS turn is again briefed to ask about access.
- 2026-09-16 round 8, photo and video on WhatsApp (a customer sends a photo of the leak, then a video of the pipe inside the 8-second quiet window, so the desk reads one turn): the live door still has no environment, so the round ran the burst through the desk's own turn window, router and composer, printing each prompt. The thread named every item by the first one's kind: the router was told "The marked turn carries 2 images", and a web form with a photo and a video attached reached the composer as "[2 images: <photo>.: <video>.]", so a reply could only thank for two photos when one was a video (answer 92: one light detail of what was sent). Fix: the router and composer threads count each kind (server/comms-v2/desk/case-file.ts `mediaCountLabel`: "1 photo and 1 video"), and the composer names which description is the photo and which the video; regression tests in router.test.ts.
- 2026-09-16 round 9, "do you cover <area>" questions on WhatsApp (a bare coverage question the router reads as scoping): the live door still has no environment, so the round ran 34 everyday coverage wordings through the desk's own coverage match and the Service desk suite. The match that makes the desk answer from the areas-covered knowledge-base row missed many of them: "Do you travel to Derby?", "Would you be able to come to Beeston?", "Are you covering Beeston?", "Does your area include Beeston?", "Is Stapleford somewhere you cover?", "Are you anywhere near Beeston?", "do you do jobs in Beeston?", "Do you cover south Nottingham?" and "Do you cover all of Nottingham?". On those the Service specialist never ran, so the reply could not say whether we cover them. Fix: server/comms-v2/service/service-tools.ts `asksAboutOurArea` now reads those wordings, with a negative test beside each new one ("does that cover all of the costs", "are you anywhere near finished", "can you do work in bathrooms"); regression tests in service-tools.test.ts and a desk-level one in service-desk.test.ts. Still unread, left alone as too ambiguous: "Do you do Beeston?", "Is Beeston covered?", "Are you in Nottingham?", "Is NG9 ok for you?", "I'm in Arnold, is that ok?".
- 2026-09-16 round 10, held drafts on Ben's board (a WhatsApp reply held by a guard, then Ben taps "Send as it stands"): the live door still has no environment, so the round ran the one-tap send through the desk's human-reply and board route suites. The held draft was rendered as if Ben had typed it: a realistic 290-character composer draft went to the customer as one WhatsApp bubble, not split at about 160 characters (answer 93). On email it went with no greeting or sign-off, and on SMS its curly quotes and dashes were not normalised. Fix: `sendHeldDraft` (server/comms-v2/desk/human-reply.ts) now renders the draft as the desk renders its own replies (`deskDraft`: default split, then the soft width if that is over the ceiling); Ben's typed answers still go as typed (answer 43). Regression tests in human-reply.test.ts (WhatsApp split with the words unchanged; email greeting and sign-off).
- 2026-09-16 round 11, pacing and STOP inside a WhatsApp burst: the live door still has no environment, so the round ran through the sandbox door's burst suite (in-process, scripted model). Pacing matched answer 91 (30 ms a character, 2.5 to 7 s a bubble, the 8 s quiet window apart), and a clock pass never messages the customer, so a STOP'd thread is not chased. But a customer who sent "Sorry wrong number" and then "STOP" inside the quiet window got a reply: the desk read the burst's joined text, "Sorry wrong number STOP", which is no whole-message keyword, so round 2's opt-out check missed it. Fix: the desk checks each message of a burst for an opt-out (server/comms-v2/desk/desk.ts `runTurn`); regression test in burst-door.test.ts (no reply, no model call).
- 2026-09-16 round 12, mixed coverage-plus-job messages on WhatsApp (such as "Are you able to cover Beeston? need a tap fixed", which the router reads as scoping only): the live door still has no environment, so the round ran 28 realistic mixed messages through the desk's own coverage check and the Service desk suite. Most were caught, but these were missed: "Are you able to cover Beeston? need a tap fixed", "do you cover beeston as well? shelves need putting up", "Do you cover beeston too? leaking tap" and "We're in Stapleford - do you cover that far out?". On those, Service never ran, so the reply took only the job half and could not say whether we cover the area. Fix: server/comms-v2/service/service-tools.ts `asksAboutOurArea` now reads "are you able to cover X", a lowercase place followed by "as well", "too" or "also" at the end of the question, and "do you cover/come/travel that far" (but not "go that far as to"). Each new wording has a test that must not match beside it ("are you able to cover the cost", "do you cover plastering too", "the crack didn't go that far"). Regression tests are in service-tools.test.ts and service-desk.test.ts. "Is Beeston too far?" is still not caught, because it is too loose to match safely.
- 2026-09-16 round 13, a single video on WhatsApp while the vision model is down (a customer sends only a video of a leaking pipe and the description fails): the live door still has no environment, so the round ran the turn through the desk suite, where the scripted vision always fails, and printed the composer brief. The composer saw only "[1 video]" and "thank for media: yes", under a rule to mention one light detail of what it shows (answer 92), so any detail it gave would be made up about a video nobody had seen. Fix: the composer thread marks an undescribed photo or video "not seen by you", and when nothing on the thread was described the brief says to thank for it plainly without saying what it shows (server/comms-v2/desk/composer.ts `mediaFor`, `buildComposerUser`). Regression tests are in desk.test.ts and router.test.ts (a photo that was described beside a video that was not).
- 2026-09-16 round 14, the WhatsApp window closing on a held thread (a customer asks "How much would that be roughly?", the desk holds for Ben and sends "Thanks, leave it with me and I'll come back to you.", and Ben reaches the card 25 hours later): the live door still has no environment, so the round ran on the in-process sandbox door (scripted model, every template approved), aged the thread with `/age`, then tried Ben's two board sends. "Send as it stands" was refused because the window was shut, which is correct (answer 34), but the held card offered no template fallback: the template button only appeared under the answer form, after Ben had typed words just to be refused. Fix: the board's held-draft card now offers the same template send when its one-tap send is refused on a shut window (client/src/pages/admin/CommsV2BoardPage.tsx `WindowTemplateOption`, shared with the answer form; the refusal clears when the customer writes again); regression test in CommsV2BoardPage.test.tsx. The template itself was then refused as "no template is true for this thread", because the desk's holding line counts as an answer, so this customer cannot be reached until they write again: OVERNIGHT-QUESTIONS.md item 3.
- 2026-09-16 round 15, a plain thanks on WhatsApp after the quote was promised, with the composer rewording the promise in everyday ways round 6 did not cover (the live door still has no environment, so the round ran 29 rewordings and answers about the quote through the desk's own repeat check and the Kiran case-study suite): 16 of 22 realistic rewordings were not read as the promise again, so a thanks would have got the quote news back. The misses called the quote an estimate, cost or figure ("I'll send the estimate over shortly"), or said it was coming their way, would follow, was expected, would be in their inbox, would be let known, or was being worked on ("Ben's on the quote now"). Separately, "You'll have to clear the cupboard first, it's not in the price." was wrongly read as a promise and would have been cut from an answer. Fix: server/comms-v2/desk/repeat.ts `isWrapUp` catches those names and wordings and no longer counts "you'll have to"; regression cases in kiran-case-study.test.ts cover the matches and six answers about the quote that must not match.
- 2026-09-16 round 16, an answered call followed by a WhatsApp message (a customer rings, Ben answers, they give the job and NG9 2AB, and Ben asks for a photo on WhatsApp; then the customer writes "Hi, it's Sam from the call earlier"). The live door still has no environment, so the round used the call's live shape (a hang-up pass with no transcript, then the `call_transcribed` pass with the call id) through the channel gateway and desk suites. At hang-up the desk ran the call reader on "(no transcript)" and read nothing. When the transcript landed it was only attached to the bubble and never read. So the job, the location and Ben's photo request never reached the file, and the next WhatsApp reply would ask for the location and a photo again (checklist 3.3). The door calls in earlier rounds carry their transcript at once, which hid this. Fix: the reader skips a call with no transcript yet, and the transcript that fills the turn in is read for facts and Ben's asks, with no reply and nothing sent (server/comms-v2/channels/channel-desk.ts `readLateTranscript`, called from channel-gateway.ts `attachCall` in turn with the file's desk passes). Regression test in channel-desk.test.ts.
- 2026-09-16 round 17, a missed call in the middle of a WhatsApp conversation (a customer writes about a dead bathroom fan, the desk replies asking for a photo, and ten minutes later the customer rings and nobody answers; then they write "Just tried ringing you. When could you come out?"). The live door still has no environment, so the round ran the thread through the channel gateway and desk suites. The missed call sent the missed-call template, "Hi Sam, sorry we missed your call. Tell us what needs doing and we will price it up for you...", to someone we had written to ten minutes earlier about the job they had already described. The old desk (the oracle for checklist 3.5) sends that text only on a first contact: `maybeAutoAckFirstContact` refuses NOT_FIRST_CONTACT when we have written to the number in the last 60 days. Fix: a missed call on a file with an outbound turn in the last 60 days gets no text back and the call stays on the board (server/comms-v2/channels/channel-desk.ts `MISSED_CALL_ONGOING_DAYS`, `ongoingSince`). Regression test in channel-desk.test.ts (no text mid-thread, the text again after 61 quiet days); an older assertion that expected the text ten minutes after a post-call SMS now expects none. The later WhatsApp reply was unaffected.
- 2026-09-16 round 18, an email enquiry and its replies (a customer emails "can you fix a dripping kitchen tap?" with an office-address signature, then replies from Gmail, Yahoo and Outlook mobile): the live door still has no environment, so the round ran realistic bodies through the email adapter (`fromInboundEmail`, `stripQuotedHistory`) and the adapter suite. The signature after the `-- ` line went to the desk as the customer's words, so its office postcode (LS1 4AB) could be read as the job's location; Gmail's reply header wrapped over two lines, Yahoo's header with the quote run onto the same line (and its "Sent from Yahoo Mail" footer) and "Get Outlook for iOS" were not recognised, so Yahoo's whole quoted history reached the desk. Fix: `stripQuotedHistory` (server/comms-v2/channels/email-adapter.ts) now also ends the new words at the signature delimiter, a wrapped or run-on "On ... wrote:" header, and the Yahoo, Outlook and Mail for Windows footers; regression test in adapters.test.ts, which also checks that body lines that only look like these ("On 3rd floor", "From 10am", "-- the side gate is open") are kept.
- 2026-09-16 round 19, opt-outs worded the everyday way on SMS and WhatsApp (a customer who writes "Don't text me again", "Wrong number, please stop" or "Stop, found someone else"): the live door still has no environment, so the round ran 51 realistic opt-out wordings and 31 job or channel-preference look-alikes through the desk's own opt-out check (`detectOptOut`) and the desk suite. 21 of the 51 opt-outs were not read as one, so the router and composer answered someone who had asked us to stop. Fix: server/opt-out-detect.ts now reads "don't text/message me again/anymore" as a full opt-out unless the message names another way to reach them ("…just call me"), texted short forms ("stop msg", "stop txt", "stop now", "stop it"), "take my number off", and a bare stop said with its reason ("wrong number", "not interested", "found someone", "not needed", "didn't ask", "who is this", "sorted"), while "Stop, the leak is getting worse" and "stop cock not needed" are still not read as opt-outs; regression tests in server/opt-out-detect.test.ts and desk.test.ts.
- 2026-09-16 round 20, a WhatsApp voice note and a shared location pin (a customer answers "whereabouts are you?" by sending a voice note, then shares a location pin for Beeston Library, Foster Ave, Beeston NG9 1AE): the live door still has no environment, so the round ran the Twilio and Meta webhook shapes through the WhatsApp adapter and the channel desk suite with a scripted model. The voice note reached the router and composer as "[1 photo or video sent that did not reach us]", so the reply could apologise for a missing photo, and the location pin arrived as an empty turn (Twilio) or "[location]" (Meta) with its address dropped; an emailed PDF was also called a failed photo. Fix: an attachment that is not a photo or video is now named on the turn ("[1 voice note sent, which you cannot open]", also document and contact card), and a shared location becomes "[location shared: <name>, <address>]" (or "a pin with no address"), with regression tests.
- 2026-09-16 round 21, a missed call followed by a WhatsApp message (a customer rings, nobody answers, they get the missed-call text, then write "Hi, just tried ringing. Kitchen tap is leaking under the sink"): the live door still has no environment, so the round ran the call and the WhatsApp turn through the channel desk with a composer that ignores the brief's "offer a call: no". The reply "…Whereabouts are you? Happy to give you a quick call back if easier." went out, because only the brief kept a customer who has already rung from being offered a call (checklist 1.5); nothing checked the reply. Fix: a composed reply that offers a call to a party who has already rung or prefers text now goes back to the composer once, and holds for Ben if it offers again (not when the customer asks to be called, or on a callback). `offersCall` no longer counts a call that already happened ("as we discussed on the phone", "we tried to call you back") as an offer, and those lines now count as questions about the job again. Regression tests are in channel-desk.test.ts and scoping-tools.test.ts
- 2026-09-16 round 22, bubble size and splitting on WhatsApp (a customer sends photos of a leaking kitchen tap, and the composer asks two things as a numbered list: "Two quick things before Ben prices it:\n1. Is the tap a mixer…?\n2. Is there an isolation valve…?"; other replies carry a Knowledge Base line such as "8 a.m. to 5 p.m." or "about 2 hrs. at most"): the live door still has no environment, so the round ran composed replies through the sender's WhatsApp renderer. Over 160 characters, the list's line breaks were folded and the reply split into bubbles ending "…prices it: 1." and "…separate taps? 2.", and a sentence was cut after "10 a.m." and "2 hrs." mid-phrase. Fix: a numbered list item keeps its line break and starts its own sentence, and "a.m.", "p.m.", "hr(s).", "min(s)." join the short forms that end a sentence only before a capital, with a regression test in sender.test.ts.
- 2026-09-16 round 23, a bulleted list in a WhatsApp or SMS reply (a customer sends photos of a leaking tap and the composer answers "Could you send me:\n- a photo of the whole tap\n- the rough size of the cupboard\nCheers"): the live door still has no environment, so the round ran realistic dash, dot and star bullet replies through the desk's own `render`. On WhatsApp the items ran into the sign-off ("the rough size of the cupboard Cheers") and "•"/"*" markers stayed mid-sentence; the SMS text went out as "send me: - a photo of the whole tap - the rough size…", with hyphens used as punctuation (house rule) because the SMS path removed dashes before it folded the lines. Fix: `bulletsAsProse` (desk/dashes.ts) turns a bulleted list into one sentence of comma-joined items, ending it with a full stop when a new capitalised sentence follows, before either channel folds lines; a person's own words are still sent as typed. Regression test in sender.test.ts (fails without the fix); comms-v2 suite 900 passed, 1 skipped.
- 2026-09-16 round 24, a customer who is not ready yet on WhatsApp (a customer writes "My bathroom tap drips", the desk asks whereabouts they are, and they answer "Thanks, I'll get back to you next month"): the live door still has no environment, so the round ran the thread through the desk suite with a composer that ignores the brief's "an acknowledgement only, no question". A repeat postcode ask was already caught by the ask ledger, but a job question ("No problem at all. Is it the hot or the cold tap that drips?") went out to a customer who had just put the job off (checklist 6.2, answers 8 and 19; the same gap applies to a promise of more, 2.5). Fix: on a not-ready or promise-of-more turn, a composed reply that asks a question now goes back to the composer once, and holds for Ben if it asks again (fixed lines are not counted). Regression test in desk.test.ts (fails without the fix); comms-v2 suite 901 passed, 1 skipped.
- 2026-09-16 round 25, bubble size on SMS with an emoji (a customer texts "Hi my bathroom extractor fan has stopped", with no WhatsApp, and the composer writes a warm reply with a 👋 and a 🙂 plus the move-to-WhatsApp line): the live door still has no environment, so the round ran realistic composer replies through the desk's own `render` and the channel desk suite. One emoji makes the whole text UCS-2, so 172 characters came to three segments and were refused. The composer had been told only "under 300 characters", and the shorten retry only "under 134", so a composer that kept its emoji ended the thread held for Ben. Fix: the SMS brief now says plain letters only, with no emoji or symbols such as × or ½. A retry for a UCS-2 text names the characters that caused it and gives the 306-character room it gets back without them (`nonGsmChars`, `ShortenBrief.wideChars`). A regression test in channel-desk, sender and router tests covers it and fails without the fix.
