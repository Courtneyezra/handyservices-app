# Brief: cost and load model (pane bottom-right)

You are one of three panes modelling a comms system BEFORE it is built, for a non-developer owner. Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md first (sections 3, 4, 5, 10 matter most). The system: one customer = one conversation across WhatsApp, SMS, calls, email, web chat; an append-only ledger; a single gated outbox (sendCustomerMessage refuses without run_id + Approver); a rules layer (first-contact acks, photo/postcode asks, 20-min silence-breaker, template chases) that SENDS from day one; narrow AI roles (Triage/Sorter on Haiku, Scoper on Sonnet at DRAFT earning SEND per intent, Quote clerk PROPOSE incl. on call transcripts, Recovery PROPOSE, Verifier = 10% sampler); exceptions always to Ben (complaints, refunds, out-of-scope, any money figure, any date); one operator per city, city = a config record. Your output is ONE self-contained HTML fragment that I will paste into a master page: a single <section id="..."> with its own scoped CSS (prefix every class) in a <style> inside the section and any JS in an IIFE. Use ONLY the colour variables in /Users/courtneebonnick/v6-switchboard/docs/comms-model/tokens.css (do not redefine them; do not hardcode colours). No external libraries. Must read well in light and dark. Do not touch any other file in the repo, do not commit, do not run the app, do not query the database. When done, reply with the output path and a 5-line summary.

## Your deliverable
`/Users/courtneebonnick/v6-switchboard/docs/comms-model/cost-model.html` — `<section id="cost">`, classes prefixed `cm-` — AND the same numbers in `/Users/courtneebonnick/v6-switchboard/docs/comms-model/cost-model.md` (tables, for the repo).

An interactive calculator with three sliders/inputs: inbound customer messages per week (default 100), inbound calls per week (default 15), number of cities (default 1, max 8), and a toggle LAUNCH (Scoper at DRAFT) / MONTH 2 (Scoper at SEND for non-exception replies). It recomputes and shows: runs per day by kind, model calls per day by model, tokens per day, £ per day and £ per month for models, and Ben taps per day (approvals + flags + 10% sample reviews) and minutes per day at 30 s per tap. Show the result at today's defaults on load, plus a small table for 1, 2 and 5 cities. Also a one-line comparison: cost of the models vs cost of Ben's time saved (assume £15/hour) so the owner can see the trade.

Volume assumptions (label them clearly as assumptions; a replay of real threads is running and may replace them): a burst = a group of inbound messages within 10 min; assume 1.6 messages per burst; 17% of bursts are exceptions (11% money, 6% dates, 0% complaints measured); 25% of bursts are first contact (rules layer); 10% are photo-only (rules layer); the rest go to the Scoper. Silence-breaker fires on ~30% of bursts at launch, ~5% at month 2. Every non-dropped burst gets one Triage call. Each answered call gets one Quote clerk call; each thread reaching 2+ Scoper bursts gets one more clerk call. Auditor reviews 10% of automatic sends.

Token assumptions per call (input/output): Triage on Haiku 4.5: 2,500 / 150. Scoper on Sonnet 5: 7,000 input of which 4,000 is a cached system prompt (cache read), 400 output. Quote clerk on Sonnet 5: 9,000 / 900 (4,000 cached). Verifier on Opus 5: 5,000 / 300. Rules layer: zero model cost.

Pricing (USD per million tokens, 2 Sep 2026): Haiku 4.5 input 1.00 output 5.00; Sonnet 5 input 2.00 output 10.00; Opus 5 input 5.00 output 25.00; cache reads at 10% of input price; cache writes at 125% (assume one cache write per 5-minute window, negligible; ignore). Convert at 1 USD = 0.78 GBP. Add Twilio/Meta message costs as a separate line: assume £0.035 per WhatsApp template send, £0 per WhatsApp reply inside the 24h window, £0.04 per SMS; assume 40% of rules-layer sends are templates, 10% are SMS.

IMPORTANT: if the file /private/tmp/claude-501/-Users-courtneebonnick-v6-switchboard/37a4e54f-b19b-4fe0-9b20-f8149bffc412/scratchpad/replay-results.json exists when you start (or when you finish, check again), read it and use its measured per-day counts (section G model calls, section E automatic sends, section D Ben taps) as the defaults instead of the assumptions above, and say so in the section. If it does not exist, use the assumptions and label them.

Keep prose under 400 words; numbers in tables; use tabular-nums.
