# Brief: data and flow diagrams (pane bottom-left)

You are one of three panes modelling a comms system BEFORE it is built, for a non-developer owner. Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md first (sections 3, 4, 5, 10 matter most). The system: one customer = one conversation across WhatsApp, SMS, calls, email, web chat; an append-only ledger; a single gated outbox (sendCustomerMessage refuses without run_id + Approver); a rules layer (first-contact acks, photo/postcode asks, 20-min silence-breaker, template chases) that SENDS from day one; narrow AI roles (Triage/Sorter on Haiku, Scoper on Sonnet at DRAFT earning SEND per intent, Quote clerk PROPOSE incl. on call transcripts, Recovery PROPOSE, Verifier = 10% sampler); exceptions always to Ben (complaints, refunds, out-of-scope, any money figure, any date); one operator per city, city = a config record. Your output is ONE self-contained HTML fragment that I will paste into a master page: a single <section id="..."> with its own scoped CSS (prefix every class) in a <style> inside the section and any JS in an IIFE. Use ONLY the colour variables in /Users/courtneebonnick/v6-switchboard/docs/comms-model/tokens.css (do not redefine them; do not hardcode colours). No external libraries. Must read well in light and dark. Do not touch any other file in the repo, do not commit, do not run the app, do not query the database. When done, reply with the output path and a 5-line summary.

## Your deliverable
`/Users/courtneebonnick/v6-switchboard/docs/comms-model/diagrams.html` — `<section id="diagrams">`, classes prefixed `dg-`. Hand-authored inline SVG only (rect, line, path, text, marker arrowheads), strokes and text in currentColor so they work in both themes, one literal colour at most per figure for the element that carries the meaning (use var(--accent) via a class on a wrapper is fine, or hardcode nothing and use currentColor + opacity). Each figure wrapped in <figure> with a <figcaption> stating its claim and role="img" + aria-label on the svg. Label every arrow with a verb (writes, reads, refuses, emits, polls). Size by viewBox, CSS max-width:100%; height:auto. Text 11-13px at drawn scale. No libraries.

Four figures:
1. **Object model.** Customer (service_client) ← contact points (phone numbers, emails) ; Customer ↔ Conversation (one per customer per city) ; Conversation → Message events (append-only ledger: message_in, message_out, call, draft_created, draft_sent, flag_raised, run_started…) ; Agent run (id, agent, pack version, case file hash, model snapshot, decision) → drafts/flags carry run_id ; Outbox row (body, channel, send_after, approver, run_id) ; City pack (number, email, operator, hours, voice, templates, tier per intent). Show cardinalities and which arrows are "derived, never stored" (outcomes joined to quotes and deposit_paid_at).
2. **Every channel, one conversation.** Five adapters on the left (WhatsApp, SMS, call recording, email, web chat/Google) each emitting the same "message arrived" event into the ledger, one conversation in the middle, the desk on the right, and outbound going back through the same adapters. Show the WhatsApp 24-hour window / template rule and SMS fallback as a labelled decision on the outbound WhatsApp edge.
3. **The exit, before and after.** Left: today, 16 modules (name six: cron, lead-automations, quotes, invoices, quick-replies, customer-notifications, + "10 more") each with their own arrow straight to Twilio/Meta, guards sitting off to the side attached only to the agent's draft path. Right: every module → one gated sendCustomerMessage(run_id, approver) → guards → ledger → adapters → Twilio/Meta. The gate should visibly refuse an arrow with no run_id (an arrow that stops at the gate with an X).
4. **Autonomy per intent, as a state machine.** States: DRAFT (Ben approves) → SEND (auto, 10% sampled) → back to DRAFT on demotion. Transitions labelled with the real criteria from §4: evals pass^3, 30 verdicts across the pack, zero unsafe; demotion: unsafe reject, opt-out, incident tag, sampled approval < 80%. A separate small box "Ben-only lane (money, dates, complaints)" with no transition into SEND, ever.

Under each figure, 2-4 sentences of caption in plain English for a non-developer. Keep the section under ~900 words of prose.
