# Phase 2 / C: eval harness + clerk & recovery on the spine (pane bottom-right)
Worktree: /Users/courtneebonnick/v6-wt-config  (branch p2-evals)

PHASE 2 of the comms rebuild ("The spine"). Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md sections 2, 3 (all), 4, 9 and 10 (Phase 2 row), then `server/spine/types.ts` in your worktree: it is the CONTRACT every Phase 2 stream builds against; extend it only by adding, never by changing existing names, and say so in your DONE file. Also read `git log --oneline -25` in your worktree for Phase 0 + Phase 1 (approver.ts, worker-gate.ts, ledger.ts, agent_runs, rules-layer.ts, working-hours.ts, draft_verdicts). Non-negotiable rules: (1) work ONLY in your worktree path; cd there for every command. (2) No dev server, no DB queries or writes except migration files (idempotent SQL in migrations/ + shared/schema.ts; never db:push), never app_settings, no push. (3) VERIFICATION: zero NEW tsc errors vs the commit you started from (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; ~1,886 pre-existing errors are expected; never run two tsc at once), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x npx vitest run`; 42 pre-existing), `npx esbuild server/index.ts --bundle --platform=node --format=esm --packages=external --outfile=/tmp/esb-$$.js` succeeds. (4) Everything you build SHIPS DARK behind `app_settings` key `spine` `{ enabled: false }` read through the same fail-closed pattern as `comms_agent`; legacy paths keep running until Phase 3 flips it. (5) Commit on your branch. (6) Write `<worktree>/PHASE2-DONE.md` (files, migrations, verification, not-done, decisions).

Also read docs/COMMS_EVALS_PLAN.md and the seed corpus at `eval-cases/seed/real-sends.json` (165 real automated sends, scrubbed: 118 legacy autosends, 24 first-contact acks, 23 from the 31 Aug unguarded incident).

## Build
1. `scripts/eval-comms.ts` — the harness from COMMS_EVALS_PLAN Phase 1: cases as JSON under `eval-cases/<family>/*.json` with shape `{ id, family, caseFile | context, expected: { lane?, intent?, mustNotContain?, mustFlag?, mustNotEscalate?, label? } }`; adapters: `legacy` (runs `runCommsAgent` in dry-run/propose mode if it has one, else skip with reason) and `spine` (runs pane A\x27s `runOnce` with the registered Scoper when present, else skip); deterministic graders first (lane match, intent ∈ allowed, guard chain pass/fail via draft-guards detectors, must-not-contain, must-flag); pass^k with k=3 for regression families; results to `eval-results/<timestamp>.json` + a scoreboard markdown with deltas vs the previous run. Runs with NO database (case files are self-contained) and no network unless `EVAL_LIVE=1`.
2. Seed families from the corpus + incidents: `guards/` (every seed send run through the detectors; the 23 incident sends must trip at least one guard or be labelled \x27unguarded_but_fine\x27 in a review file you write for the owner), `first_contact/`, `money_question/` (must flag), `date_question/`, `complaint/`, `incident_regressions/` (James triple-send 27 Aug: near-duplicate + malformed reason; Alicia survey-required; Harbans AM/PM; free-visit promise; duration claim), `absence/` (ordinary scoping replies that must NOT escalate). ~40 cases total. Report the guard chain\x27s measured false-negative rate on the incident set (design §9).
3. Port Quote clerk and Recovery onto the spine as `SpineAgent`s (tier PROPOSE) in `server/spine/agents/quote-clerk.ts` (wraps server/agents/quote-prep.ts; emits `category` per line; triggered by tag `needs_quote` or `call_ended` with a transcript) and `server/spine/agents/recovery.ts` (wraps recovery.ts candidates → nudge_queue). Register both in `server/spine/agents/index.ts` (create if pane B has not). Neither sends; both write agent_runs.
4. Remove Ops Manager\x27s direct comms bypass: `server/agents/ops-manager.ts` `run_comms_agent` → calls `requestRun` from server/spine/request-run.ts when `spine.enabled`, else legacy. Coordinate by importing only the names in types.ts / request-run.ts; if pane A\x27s file is absent in your worktree, write the call behind a dynamic import with a clear TODO and note it.
5. Tests: vitest for graders and the scoreboard delta; the harness itself runs green on the seed families with the `legacy` adapter skipped.
