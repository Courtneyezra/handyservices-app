# Phase 4 / B: in-chat quote card (pane bottom-left)
Worktree: /Users/courtneebonnick/v6-wt-worker  (branch p4-quote-card)

PHASE 4 of the comms rebuild ("Widen"). Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md sections 3.4, 3.5, 3.8, 7 and 10 (Phase 4 row), then `git log --oneline -60` in your worktree and `server/spine/` (Phases 2–3). Non-negotiable rules: (1) work ONLY in your worktree path. (2) No dev server; no DB access except migration files (idempotent SQL + shared/schema.ts; never db:push); never app_settings; no push; no external API calls unless the brief says a script may with an explicit flag. (3) VERIFICATION: zero NEW tsc errors vs your start commit (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; one tsc at a time), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x npx vitest run`; 42 pre-existing), esbuild bundle of server/index.ts succeeds. (4) Ships dark behind `app_settings.spine` fields defaulting off. (5) Commit on your branch. (6) Write `<worktree>/PHASE4-DONE.md`.

Also read the locked spec in the memory note summarised here: compact review card in the thread panel (not navigation to the builder, not an embedded full builder); Save = save DRAFT quote, send later; missing name/postcode → the Scoper drafts the ask (now the rules layer\x27s `sendAsk`); media on the quote = tickable thumbnails, all ticked by default. Customer type defaults homeowner, inferred from messaging (landlord / letting agent / business signals).

## Build
1. Server: `GET /api/spine/quote-intake/:conversationId` returns the latest Quote clerk `Proposal.artifact` (kind \x27quote_intake\x27) from agent_runs for the thread (or 404 with `{ available:false }`), plus the thread\x27s media list. `POST /api/spine/quote-intake/:conversationId/save-draft` takes `{ lines[{title, category, qty?, notes?}], customerType, name, postcode, mediaIds[] }` and saves an UNSENT draft quote using whatever draft mechanism the builder already has (search client/src/components/quote-builder and server/quotes.ts for draft/unsent status; if none exists add `status: 'draft'` handling to personalized_quotes via a migration ONLY if a status column does not exist — check first). Never prices; `price` fields stay null for Ben.
2. Client: `client/src/components/comms/QuoteIntakeCard.tsx` rendered in ThreadPanel (CommsPage.tsx) when an intake exists: name, postcode, customer type (select), job lines (editable title/category, add/remove), media thumbnails with tick boxes (all ticked), "Save draft quote" and "Open full builder" (deep link with the same prefill). "Waiting on postcode" / "waiting on name" chips when missing, with a "Ask now" button that calls the rules layer ask endpoint (`POST /api/spine/ask/:conversationId { kind: 'ask_postcode'|'ask_media' }` → server/rules-layer.ts `sendAsk`, human approver from the session).
3. Trigger: when the Scoper proposes tags `needs_quote` or a call ends with a transcript, the spine already runs the clerk (Phase 2). Make the card refresh over the existing comms SSE bus when a new quote_intake run lands (server: publish an event from the spine exit for artifact proposals).
4. Tests: vitest for the intake→draft mapping (pure), and a component smoke test if the client test setup exists (else skip with a note).
