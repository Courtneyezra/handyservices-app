# Phase 2 / B: the Scoper (pane bottom-left)
Worktree: /Users/courtneebonnick/v6-wt-worker  (branch p2-scoper)

PHASE 2 of the comms rebuild ("The spine"). Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md sections 2, 3 (all), 4, 9 and 10 (Phase 2 row), then `server/spine/types.ts` in your worktree: it is the CONTRACT every Phase 2 stream builds against; extend it only by adding, never by changing existing names, and say so in your DONE file. Also read `git log --oneline -25` in your worktree for Phase 0 + Phase 1 (approver.ts, worker-gate.ts, ledger.ts, agent_runs, rules-layer.ts, working-hours.ts, draft_verdicts). Non-negotiable rules: (1) work ONLY in your worktree path; cd there for every command. (2) No dev server, no DB queries or writes except migration files (idempotent SQL in migrations/ + shared/schema.ts; never db:push), never app_settings, no push. (3) VERIFICATION: zero NEW tsc errors vs the commit you started from (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; ~1,886 pre-existing errors are expected; never run two tsc at once), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x npx vitest run`; 42 pre-existing), `npx esbuild server/index.ts --bundle --platform=node --format=esm --packages=external --outfile=/tmp/esb-$$.js` succeeds. (4) Everything you build SHIPS DARK behind `app_settings` key `spine` `{ enabled: false }` read through the same fail-closed pattern as `comms_agent`; legacy paths keep running until Phase 3 flips it. (5) Commit on your branch. (6) Write `<worktree>/PHASE2-DONE.md` (files, migrations, verification, not-done, decisions).

## Build
1. `server/spine/agents/scoper.ts` — a `SpineAgent` named \x27scoper\x27 on `server/agents/runner.ts` (Sonnet 5 via server/llm.ts, prompt caching on the system block, run id threaded through). Tool belt, and NOTHING else: `propose_reply(intent, body[], reasons[], citations?)` (one call ends the run), `flag(exception, note)`, `set_contact_name(name)` (reuse the placeholder-name rejection from comms.ts), `schedule_recontact(date, note)` (PROPOSE into nudge_queue as today), `get_quick_replies()`. No get_thread/get_customer_context: the CaseFile is injected as the user turn. Intents limited to the pack\x27s `allowedIntents`; any other intent name is rejected at the tool boundary.
2. Standing orders: split the SYSTEM prompt in server/agents/comms.ts into `server/spine/prompts/scoper.core.md` (behaviour, hard rules, greet once, belief hygiene, deliverability-first, flag charter) and pack voice files under `brand-voice/` (customer.default → whatsapp-comms.md as is). Money/dates/discounts/durations are not just forbidden in prose; the belt cannot express them, and the guard bridge (pane A) checks the text anyway. Target ≤ 2.5k tokens for the core prompt (today ~6k).
3. Post-quote behaviour (`customer.post_quote`): port `postQuoteStandingOrders()` and the objection-levers vocabulary (server/agents/objection-levers.ts) as a second prompt fragment selected by pack.
4. Quote clerk hook: when the Scoper judges the job scoped, it returns `propose_reply` with intent \x27quote_on_its_way\x27 AND tags `needs_quote` via the proposal (add an optional `tags?: string[]` to Proposal in types.ts, additive). Pane C wires the clerk.
5. `server/spine/agents/scoper.test.ts`: run the agent against 8 synthetic CaseFiles with a stubbed runner (no network): first contact with photos, mid-scope answer, money question (must flag, never reply with a figure), date question (point_to_picker only when a quote exists, else flag), complaint (flag, no apology admitting fault), returning customer, opted-out (must return null), post-quote objection. Assert intents ∈ allowedIntents and bodies pass the guard bridge if pane A\x27s guards.ts exists in your worktree (else run draft-guards detectors directly).
6. Register the agent in `server/spine/agents/index.ts` so pane A\x27s registry can pick it up; keep a thin adapter so `runCommsAgent` (legacy) is untouched.
