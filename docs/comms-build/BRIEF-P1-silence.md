# Phase 1 / B: due times, holding line, silence-breaker (pane bottom-left)
Worktree: /Users/courtneebonnick/v6-wt-worker  (branch p1-silence, created by the orchestrator from comms-v3)

PHASE 1 of the comms rebuild ("See everything, never go silent"). Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md sections 2, 3.5, 3.7, 4, 8 and 10 (Phase 1 row) first, then `git log --oneline -15` in your worktree to see what Phase 0 added (server/approver.ts with the Approver union + newRunId, server/worker-gate.ts with isCommsWorker, the gated sendCustomerMessage). Non-negotiable working rules: (1) work ONLY inside your worktree path; cd there for every command; never edit the main checkout or another worktree. (2) Do not run the dev server; do not run queries against the database except through migration files you write (the orchestrator applies migrations); never touch app_settings; do not push. (3) Migrations: write a dated SQL file in migrations/ (idempotent: IF NOT EXISTS / ADD COLUMN IF NOT EXISTS) AND the matching Drizzle change in shared/schema.ts. Never run db:push. (4) node_modules is symlinked. VERIFICATION RULE (learned in Phase 0): `npm run check` has 1,887 pre-existing type errors and vitest has 42 pre-existing failures (eve-pricing-engine, segment-classifier, contractor-pay). Your gate is: ZERO NEW tsc errors versus the baseline commit you started from (run `NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort` on both and diff, ignoring line numbers), and vitest failures identical to baseline, run with `DATABASE_URL=postgres://u:p@127.0.0.1:1/x` since the worktree has no .env. Never run tsc in more than one shell at a time; it shares a tsbuildinfo under node_modules. Do not fix pre-existing errors outside your files. (5) Commit on your branch. (6) When finished write `<worktree>/PHASE1-DONE.md`: files changed, migrations to apply (exact filenames), how you verified, anything not done and why, decisions the design left open.

## Build
1. **due_at.** Migration + schema: `message_drafts.due_at timestamptz null`, `agent_questions.due_at timestamptz null`, `agent_questions.expired_at timestamptz null`, `message_drafts.held_reason text null`. Defaults set at creation: drafts due 4 working hours (Mon–Fri 08–18 Europe/London; outside that, next working slot); flags due 4 working hours, 20 minutes for `callback_requested`/urgent. Put the working-hours arithmetic in `server/working-hours.ts` (one implementation; replace the ≥4 inline UK-hour computations the audit found in comms.ts, comms-sweep.ts, sla-sweep.ts, first-contact-ack.ts with it).
2. **Rules layer module.** New `server/rules-layer.ts` that owns content-free sends: `sendHoldingLine(conversationId, kind: 'silence'|'flag_expiry'|'draft_expiry', runId)` and `sendAsk(conversationId, kind: 'ask_media'|'ask_postcode', runId)`. Copy in Handy voice, no em dashes, no prices/dates, "we" not "I" (read brand-voice/whatsapp-comms.md and shared/chat-voice.ts). Reuse first-contact-ack.ts template/window/SMS ladder logic (approved Meta template by name if the 24h window is shut, else freeform, else SMS) rather than duplicating it; approver `rules.first_contact` or add `rules.holding` to the Approver union. Every send passes through sendCustomerMessage with runId. Suppression: never send if ANY outbound landed on the thread after the triggering inbound, if the customer opted out, or if a holding line already went in the last 2 hours.
3. **Silence-breaker.** In the worker fast tick (gated by isCommsWorker): find threads whose last inbound is ≥ 10 min old with no outbound since → sendHoldingLine('silence'). Idempotent via a `metadata.silenceBreakerAt` stamp or a ledger lookup. 24/7.
4. **Expiry.** Same tick: flags past due_at and unanswered → sendHoldingLine('flag_expiry'), set expired_at, re-ping Ben once (existing Pushover helper); pending drafts past due_at → sendHoldingLine('draft_expiry') and set held_reason='due_expired' (draft stays pending). Ledger events for these are appended via server/ledger.ts if it exists in your worktree, else via logSystemEvent (pane A owns the ledger; do not block on it).
5. **Digest.** 09:00 Europe/London cron (gated): Pushover with counts of flags past due, pending drafts > 2h, and bursts that got only a holding line yesterday.
6. **Tests.** vitest for working-hours arithmetic (due_at across weekend, Friday 17:30, bank of cases) and suppression rules using fakes. tsc clean.

## Done when
No inbound can sit > 10 min without an outbound, flags and drafts expire into a holding line, one working-hours module, tests green, migrations listed.
