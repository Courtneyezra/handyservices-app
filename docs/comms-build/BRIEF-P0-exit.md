# Phase 0 / A: the single exit (pane top-right)
Worktree: /Users/courtneebonnick/v6-wt-exit  (branch comms-v3/exit)

PHASE 0 of the comms rebuild ("Close the doors"). Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md sections 1.2, 3.1, 3.6, 3.9 and 10 (Phase 0 row) first. Non-negotiable working rules: (1) work ONLY inside your worktree path below; cd there for every command; never edit /Users/courtneebonnick/v6-switchboard itself or any other worktree. (2) Do not run the dev server, do not query or modify the database, do not touch app_settings, do not push. (3) node_modules is symlinked; run `npm run check` (tsc) and `npx vitest run` in your worktree and make both pass before you finish. (4) Commit on your branch with clear messages. (5) When finished write `<worktree>/PHASE0-DONE.md` containing: files changed, what you verified and how, anything you could not do and why, and any decision you made that the design did not specify. The orchestrator merges your branch; do not merge or rebase yourself.

## Build
1. **Approver enum.** New file `server/approver.ts` exporting a string-literal union `Approver` (never a bare string): `agent.comms`, `agent.comms.autosend`, `agent.sla_chase`, `rules.first_contact`, `rules.hours_gate`, `rules.post_call`, `system.invoice`, `system.notification`, `system.webform_chase`, `system.quick_reply`, `system.voice_note`, `system.template_sync`, `system.cron`, `system.landlord_portal`, `system.quotes`, `system.daily_planner`, `system.lead_automation`, `system.live_call`, `system.staff`, plus `human:<id>` as a template literal type. Export `isAutomatedApprover(a: string): boolean` (true for anything not starting `human:`; ALSO true for the legacy stored prefixes `comms_agent:`, `hours_gate:`, `first_contact_ack:`, `v2_pipeline:` so old rows keep behaving). Export `newRunId(prefix?: string): string` (e.g. `run_<uuid>`).
2. **Replace the regex.** In `server/message-drafts.ts` delete `AUTOMATED_APPROVER` (line ~264) and use `isAutomatedApprover`. Change `approveAndSendDraft(id, approvedBy: string, ...)` to take `approver: Approver`. Update its 8 importers (server/agents/comms.ts, comms-sweep.ts, sla-sweep.ts, promise-tracker.ts, server/auto-ack-window.ts, server/first-contact-ack.ts, server/outbound.ts, server/agent-staff.ts) to pass enum values. Map old strings: `comms_agent:autosend` → `agent.comms.autosend`, `comms_agent:sla_chase` → `agent.sla_chase`, `hours_gate:morning_release` → `rules.hours_gate`, `first_contact_ack:*` → `rules.first_contact`, human approvals → `human:<id>`. The stored `approved_by` value becomes the enum string.
3. **The gate.** In `server/outbound.ts` add REQUIRED fields to `SendCustomerMessageInput`: `approver: Approver` and `runId: string`. At the very top of `sendCustomerMessage` (before the opt-out rule), if either is missing or runId is not a non-empty string, return `{ ok:false, error:'MISSING_RUN_ID_OR_APPROVER', attempts:[], fellBack:false }`, `console.error` it, and `logSystemEvent({kind:'send_refused', ...})` (helper in server/system-events.ts). Record `approver` and `runId` on the outbound message row it writes, in the messages metadata/jsonb column if one exists (check shared/schema.ts; if none, add nothing to the schema in this phase and note it in DONE).
4. **Migrate every caller.** `grep -rn "sendCustomerMessage(" server` → 16 files besides outbound.ts (agent-staff, cron, customer-notifications, daily-planner-routes, invoice-generator, invoices, landlord-portal, lead-automations, live-call-actions, message-drafts, quick-replies, quotes, services/webform-chase-service, voice-notes, whatsapp-api, whatsapp-template-sync). Each passes the right `system.*`/`rules.*`/`agent.*` approver and a runId: use the agent run id where one exists in scope, else `newRunId('sys')`. message-drafts.ts passes the draft's approver through. tsc must be clean, which proves no caller was missed.
5. Do NOT touch comms-sweep.ts beyond the approver rename (another pane is removing the V2 branch there).

## Done when
tsc clean, vitest green, every `sendCustomerMessage(` call in server/ has approver + runId, no `AUTOMATED_APPROVER` left, PHASE0-DONE.md written.
