# P7 / A: no reply may go out that was written before the customer's latest message (pane top-right)
Worktree: /Users/courtneebonnick/v6-wt-exit  (branch p7-stale-drafts, from comms-v3)

Real incident, 2 Sep, thread 46a13bdb… (Janet): at 14:14 she wrote "back soon with measurement"; at 14:15 the legacy agent queued a draft saying "just send the measurement over whenever you've got it"; at 14:28 the measurement photo arrived. The draft stayed pending because `queueDraft`'s source dedupe refuses a second pending draft for the same number, so the 14:28 run produced nothing, and Ben was one tap from sending a reply that asked for something she had already sent. Nothing at approval or at the exit checks whether the customer has written since the draft was made. This must be impossible for BOTH the legacy agent (still live) and the spine (shadow → live from 4 Sep), for human taps and for autosend.

Rules: work only in your worktree; no dev server, no DB except idempotent migration files (never db:push), never app_settings, no push; verification = zero new tsc errors vs your start commit (~1,872 pre-existing), vitest failures identical to baseline (42, `DATABASE_URL=postgres://u:p@127.0.0.1:1/x PHASE0_MERGED=1 npx vitest run`), client project green (`npm run test:client`), esbuild bundle OK; commit on your branch; write `<worktree>/P7-DONE.md`.

## Build
1. **Exit-time freshness guard (the hard guarantee).** In `approveAndSendDraft` (server/message-drafts.ts), before any send: read the latest non-quarantined inbound `created_at` (and id) for the draft's conversation; if it is later than `message_drafts.created_at` (or than a new column `message_drafts.based_on_inbound_id` when set), do NOT send: revert to `pending` with `held_reason = 'stale_by_inbound'`, log a `system_events` row and a ledger `draft_rejected`-style event with reason, and ask for a fresh run (`requestRun(conversationId, 'inbound_message', { delayMs: 0 })` when the spine is on, else set `metadata.nextTriageAt = now`). Applies to every approver, human or automated. Migration: `message_drafts.based_on_inbound_id text null` (+ `stale_by_inbound` documented on held_reason); `queueDraft` stamps it from the case file / thread at creation.
2. **Supersede on new inbound.** When a new inbound message or media lands (the ingest points: `server/meta-whatsapp.ts`, `server/conversation-engine.ts`, `server/whatsapp-ingest.ts` helpers), any PENDING draft on that thread with source `comms_agent` or `spine` created before it is marked `rejected` with `approved_by = 'system:stale_by_inbound'` and a ledger event, so the dedupe can never block the fresh draft. Rules-layer drafts are not touched. Put the logic in one function `supersedeStaleDrafts(conversationId, inboundAt)` in message-drafts.ts and call it from the ingest helper that both webhooks share; also call it at the start of `queueDraft` for agent sources so an agent run that starts after a new inbound clears the old draft itself.
3. **Spine exit re-check.** In `server/spine/exit.ts`, on a `send` decision, re-read the latest inbound id at send time and compare with `caseFile.timeline`'s last inbound; if newer → do not send, queue as pending with `held_reason 'stale_by_inbound'` and `requestRun` again. Also stamp `based_on_inbound_id` on every draft the spine queues.
4. **"Customer promised more" wait.** Triage (rules-first) gains a `customer_promised_more` signal: regex over the last inbound for "back soon", "will send", "sending now", "one sec", "hang on", "bear with", "give me a minute", "let me get", "I'll get you", "in a minute", "shortly" (customer side only). When set and no media/message has arrived since, `decide` returns `{ kind:'none', reason:'waiting_for_promised' }` and `requestRun(conversationId, 'inbound_message', { delayMs: 15 * 60_000 })` schedules a follow-up; if the promised item arrives, the normal inbound path runs earlier. Never a flag, never a holding line for this case (the holding line at 10 min is still fine, it does not ask for anything). Expose `lastInboundPromisedMore` on the CaseFile (additive).
5. **Ben's UI.** In `DraftApprovalCard` (CommsPage.tsx) show a red "Customer wrote since this draft (N messages / media)" banner when the thread has inbound newer than the draft, and disable one-tap approve (edit still allowed; a "Re-draft" button calls `POST /api/spine/rerun/:conversationId` → requestRun, or the legacy equivalent). The approve route returns 409 `STALE_BY_INBOUND` in that case; the client shows it.
6. **Triage fixes seen on the same thread:** (a) the Haiku triage output failed the zod schema twice ("reasons.0 String must contain at most 200 chars") → truncate/clamp `reasons` to 200 chars instead of failing to rules; (b) "That's the only cladding, back soon with measurement" was routed to the Ben lane by the date lexicon: find the offending token in `RE_DATE` (likely "soon") and remove it; add both as eval cases under `eval-cases/absence/` and `eval-cases/date_question/`.
7. Tests: unit tests for the freshness guard (draft older than inbound → held; newer → sends), supersede on inbound (legacy + spine sources rejected, rules_layer untouched), promised-more detection + decide wait, and the schema clamp. Client test for the banner + disabled approve.
