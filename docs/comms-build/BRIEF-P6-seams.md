# Close-out / A: finish the seams the panes left open (pane top-right)
Worktree: /Users/courtneebonnick/v6-wt-exit  (branch p6-seams)

CLOSE-OUT pass on the comms rebuild (after Phases 0–5 first pass). Read docs/comms-build/HANDOVER.md, docs/comms-build/CUTOVER.md and the "Not done" sections of docs/comms-build/P*-DONE.md in your worktree first; production is on this code with the spine in SHADOW, so nothing you build may change customer-facing behaviour unless it is behind an existing off-by-default switch. Rules: (1) work ONLY in your worktree; cd there for every command. (2) No dev server; no DB access except idempotent migration files (never db:push); never app_settings; no push. (3) VERIFICATION: zero NEW tsc errors vs your start commit (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; ~1,876 pre-existing; one tsc at a time), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x PHASE0_MERGED=1 npx vitest run`; 42 pre-existing), esbuild bundle of server/index.ts succeeds. (4) Commit on your branch. (5) Write `<worktree>/P6-DONE.md` (files, migrations, verification, not-done, decisions).

## Build
1. **Template-first exit** (P2-spine not-done): in `server/spine/exit.ts`, when the decision is SEND and the WhatsApp window is shut, resolve the pack's template for the intent (`pack.templates[intent]`, via the same approved-template lookup the rules layer uses) and queue the draft with `contentSid`/variables so `approveAndSendDraft` sends the template; only if no approved template exists fall to the current OUTSIDE_WINDOW → pending path (never silent). Tests with fakes.
2. **Ledger drift check on cron** (P1-ledger not-done): worker-gated nightly 03:30 Europe/London calling `ledgerDriftCheck()`; Pushover (existing `escalation` or `worker_health` key) if drift > 0; log a `system_events` row either way.
3. **Parent run** (P2-spine not-done): migration `agent_runs.parent_run_id text null` + index; the spine's triage run and agent run of one pass share it; AgentRunsDrawer groups children under the parent (client/src/components/comms/AgentRunsDrawer.tsx) — small change.
4. **Human promote/demote** (P3-autonomy not-done): `POST /api/spine/tiers { packId, intent, tier, reason }` (requireAdmin) writing `pack_intent_tiers` + `pack_tier_events` with `changed_by = human:<id>`, refusing SEND for intents outside the pack or any money/date name; a small button pair on the staff page ladder rows.
5. **Judge agreement** (P3-sampler not-done): `verdictStats` gains `sampler: { judged, humanReviewed, agreement }` from `draft_verdicts` rows sharing a `draft_id` between `agent.verifier` and `human:*`; show on the verifier staff card.
