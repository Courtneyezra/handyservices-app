# Phase 5 / A: delete the V2 pipeline and its dependencies (pane top-right)
Worktree: /Users/courtneebonnick/v6-wt-exit  (branch p5-delete-v2)

PHASE 5 of the comms rebuild ("Delete"), first pass = everything that is safe BEFORE the legacy agent is retired. Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md §10 (Phase 5 row) and §11, and docs/comms-build/PHASE5-DELETE.md (in the main checkout), then `git log --oneline -80` in your worktree. Non-negotiable rules: (1) work ONLY in your worktree path. (2) No dev server; no DB access; migration files only if the brief says so (never db:push); never app_settings; no push. (3) VERIFICATION: zero NEW tsc errors vs your start commit (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; ~1,882 pre-existing; one tsc at a time; deleting files may REMOVE errors, that is fine), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x PHASE0_MERGED=1 npx vitest run`; 42 pre-existing), esbuild bundle of server/index.ts succeeds. (4) The legacy comms agent (server/agents/comms.ts and its sweeps) MUST keep working: the spine is only in shadow mode in production. (5) Commit on your branch. (6) Write `<worktree>/PHASE5-DONE.md`.

## Build
1. Delete: `server/pipeline/v2.ts`, `server/workers/*`, `server/memory/*` (and any `conversation-memory` helper), `shared/conversation-memory.ts`, `server/llm/openrouter.ts` and `server/llm/router.ts` IF nothing outside the deleted set imports them (grep first; `server/llm.ts` stays), `scripts/_smoke-vision-video.ts`, `scripts/_test-reply-worker.ts`, the `_case-study` script the 31 Aug trace commit added (find it), and the `conversation_memory` table from `shared/schema.ts`. Leave `server/pipeline/actions.ts` and `queries.ts` (live ops code, wrong directory name; rename the directory to `server/ops/` and fix its 4–5 importers ONLY if tsc/esbuild stay clean; else leave and note).
2. Move `docs/AGENT_FRAMEWORK_V2_PLAN.md` and `docs/AGENT_FRAMEWORK_V2_ARTIFACT.md` to `docs/archive/` with a one-line header saying they are superseded by COMMS_AGENTS_V3_DESIGN.md.
3. Migration `migrations/20260903_drop_conversation_memory.sql` containing ONLY a commented-out `-- DROP TABLE IF EXISTS conversation_memory;` plus a note that the orchestrator applies it by hand after confirming the table is empty (never auto-apply a drop).
4. Architecture test: add an assertion that none of the deleted module paths exist and nothing imports `openrouter`.
5. Remove the `OPENROUTER_API_KEY` mention from any docs/env examples (`.env.example` if present) and from PHASE0-OPS.md.
