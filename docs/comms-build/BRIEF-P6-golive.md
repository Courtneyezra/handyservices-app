# Close-out / C: go-live readiness and data hygiene (pane bottom-right)
Worktree: /Users/courtneebonnick/v6-wt-config  (branch p6-golive)

CLOSE-OUT pass on the comms rebuild (after Phases 0–5 first pass). Read docs/comms-build/HANDOVER.md, docs/comms-build/CUTOVER.md and the "Not done" sections of docs/comms-build/P*-DONE.md in your worktree first; production is on this code with the spine in SHADOW, so nothing you build may change customer-facing behaviour unless it is behind an existing off-by-default switch. Rules: (1) work ONLY in your worktree; cd there for every command. (2) No dev server; no DB access except idempotent migration files (never db:push); never app_settings; no push. (3) VERIFICATION: zero NEW tsc errors vs your start commit (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; ~1,876 pre-existing; one tsc at a time), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x PHASE0_MERGED=1 npx vitest run`; 42 pre-existing), esbuild bundle of server/index.ts succeeds. (4) Commit on your branch. (5) Write `<worktree>/P6-DONE.md` (files, migrations, verification, not-done, decisions).

## Build
1. **`scripts/_golive-check.ts`** — runs CUTOVER §0 preconditions and prints a go/no-go table: health endpoint 200 + fresh heartbeat + worker role; `to_regclass` for the five comms tables/columns; approved templates by name (`holding_line_v1`, `missed_call_ack`, the video/postcode/call requests) via the template sync module's cached read; `eval-comms` regression red = 0 (spawn it); spine mode; `comms_agent` flags; count of shadow runs and errors in the last 24 h; open flags past due. Read-only (SELECTs only), runs against DATABASE_URL, exits non-zero on any NO-GO. Also `--json`.
2. **Shadow → live helper**: `scripts/_golive.ts --to shadow|live --yes` that performs CUTOVER §3 steps 1–3 in order with a confirmation of each (spine mode, `asks.enabled`, `comms_agent.onInbound=false`), writing a system_events row per step, and `--rollback` that reverses them (CUTOVER §4). Uses existing `setSpineConfig` / `setCommsAgentConfig`. Do not run it.
3. **conversation_memory export**: `scripts/_export-conversation-memory.ts` writes every row to `server/storage/archive/conversation_memory-<date>.json` (gitignored path) so the orchestrator can apply the commented DROP afterwards. Read-only; do not run.
4. **Double-ping guard** (P1-silence not-done): in `server/agents/sla-sweep.ts`, skip the `needs_ben` Pushover when the thread has an open `agent_questions` row with a `due_at` (the flag-expiry path already pings). Test with fakes.
5. **Test contractor row**: `scripts/_retire-test-contractor.ts --dry-run` listing the Ofcom-numbered contractor (Mike Thompson, 07700900001) and what would be deactivated (`isActive=false`, phone cleared) with `--apply`; do not run.
