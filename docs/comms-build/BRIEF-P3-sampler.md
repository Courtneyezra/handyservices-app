# Phase 3 / B: the sampler and the switch (pane bottom-left)
Worktree: /Users/courtneebonnick/v6-wt-worker  (branch p3-sampler)

PHASE 3 of the comms rebuild ("Earn sending"). Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md sections 4, 5, 9 and 10 (Phase 3 row) and section 0b (fast track), then `git log --oneline -40` in your worktree and `server/spine/` (Phase 2: request-run, case-file, triage, packs, guards, decide, exit, agents/scoper, agents/quote-clerk, agents/recovery, the eval harness scripts/eval-comms.ts and eval-cases/). Non-negotiable rules: (1) work ONLY in your worktree path. (2) No dev server; no DB access except migration files (idempotent SQL + shared/schema.ts; never db:push); never app_settings; no push. (3) VERIFICATION: zero NEW tsc errors vs your start commit (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; one tsc at a time), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x npx vitest run`; 42 pre-existing), esbuild bundle of server/index.ts succeeds. (4) Nothing you build changes production behaviour by itself: promotion/demotion and the sampler ship behind `app_settings` key `spine` fields (`enabled`, `autonomy.enabled`, `sampler.enabled`) that default false, fail closed. (5) Commit on your branch. (6) Write `<worktree>/PHASE3-DONE.md` (files, migrations, verification, not-done, decisions).

## Build
1. **Sampler** `server/spine/agents/verifier.ts` as a READ-tier SpineAgent + `server/spine/sampler.ts`: every morning 08:30 Europe/London (worker-gated, behind `spine.sampler.enabled`) pick 10% (min 1, max 15) of yesterday's automatic sends (agent_runs where decision.kind = 'send' and approver starts `agent.`), plus every send whose thread got an opt-out, a complaint keyword or no reply in 48h after a question. For each: an Opus 5 (server/llm.ts) rubric call `move-quality-v1` (was the move right, was the voice right, anything unsafe) → `draft_verdicts` row with verdict `sample_fine` / `sample_not_fine` + reason, by `agent.verifier`, AND a queue item for Ben: reuse `agent_questions` with source `sampler`, options [fine, not fine], due_at next office day, so Ben's tap overrides the judge (verdict `sample_fine`/`sample_not_fine` by `human:`). Judge agreement with Ben is computed by pane A's stats later; store both.
2. **Morning strip in /admin/comms**: a compact "Yesterday's automatic sends to check" list above the board (client/src/pages/admin/CommsPage.tsx) with one-tap fine / not fine (reason chips on not fine). Uses the existing agent_questions answer endpoint.
3. **The switch** `server/spine/switch.ts`: `spineMode()` reads `app_settings.spine` → 'off' | 'shadow' | 'live'. Wire into comms-lanes / comms-sweep / ops-manager (pane A of Phase 2 put the flag check in; make it a three-way): off = legacy only; shadow = spine runs `runOnce` with exit forced to 'none' and records agent_runs + a `shadow_decision` field, legacy still drafts; live = spine only, legacy `runCommsAgent` never called. `scripts/_spine-mode.ts --off|--shadow|--live` flips it (writes app_settings via setSetting; log a system_event). Default off.
4. **Shadow report** `scripts/_shadow-report.ts`: for the last N days compare spine shadow decisions vs what legacy actually did on the same thread/turn (lane, intent, sent/pending/flag, guard hits) → markdown table + agreement %. This is the Phase 2 exit evidence.
5. Tests: sampler selection logic with fakes; spineMode parsing; shadow comparison over a fixture.
