# Phase 5 / C: operator handover and staff directory (pane bottom-right)
Worktree: /Users/courtneebonnick/v6-wt-config  (branch p5-handover)

PHASE 5 of the comms rebuild ("Delete"), first pass = everything that is safe BEFORE the legacy agent is retired. Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md §10 (Phase 5 row) and §11, and docs/comms-build/PHASE5-DELETE.md (in the main checkout), then `git log --oneline -80` in your worktree. Non-negotiable rules: (1) work ONLY in your worktree path. (2) No dev server; no DB access; migration files only if the brief says so (never db:push); never app_settings; no push. (3) VERIFICATION: zero NEW tsc errors vs your start commit (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; ~1,882 pre-existing; one tsc at a time; deleting files may REMOVE errors, that is fine), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x PHASE0_MERGED=1 npx vitest run`; 42 pre-existing), esbuild bundle of server/index.ts succeeds. (4) The legacy comms agent (server/agents/comms.ts and its sweeps) MUST keep working: the spine is only in shadow mode in production. (5) Commit on your branch. (6) Write `<worktree>/PHASE5-DONE.md`.

## Build
1. `docs/comms-build/HANDOVER.md` — for Courtnee (owner) and Ben (operator), plain English: what shipped in Phases 0–4 and what each switch does (`spine.enabled/shadow/mode`, `asks`, `autonomy`, `sampler`, `video`, `comms_agent.autosend`, `comms_agent.onInbound`), the current production state (spine SHADOW since 3 Sep ~00:15 UK, legacy drafting, autosend off), the daily routine for Ben (queue, flags with deadlines, morning sample strip, reason chips), the weekly routine for Courtnee (`scripts/_shadow-report.ts --days 7`, `scripts/_autonomy-report.ts --dry-run`, eval scoreboard, /admin/staff ladder), the owner actions still open (submit `holding_line_v1` with `scripts/_submit-holding-template.ts --submit`; enter 8 contractor phones; Neon dev branch + strip Twilio secrets from laptops; kill the six stale `tsx watch` processes; agree Ben's hours/pay), and the go-live steps copied from CUTOVER.md §3 with the date they become eligible.
2. `/admin/staff` (server/agent-staff.ts + AgentStaffPage.tsx): add STAFF cards for the spine roles that lack one (triage, scoper, quote_clerk, recovery, verifier, contractor_liaison, vision, rules layer) following the existing `STAFF` export convention (mission, autonomy ladder freely/approval/never, tool belt chips, model, cadence), mark the legacy comms card "retiring after 7 live days", and show the spine mode + per-switch state at the top of the page (read from `/api/agents/staff`, which should include `spine` config sans secrets).
3. Update `docs/RUNBOOK.md` with a "Comms desk" section: health endpoint, spine mode commands, rollback, where runs/verdicts/ledger live, the migration applier, and the verification rule for builds (no new tsc errors vs baseline; vitest 42 baseline; esbuild).
4. Update `CLAUDE.md` (repo root) "Session Notes"/"Current Work in Progress" with a 10-line summary of the comms desk and pointers to COMMS_AGENTS_V3_DESIGN.md, CUTOVER.md, HANDOVER.md; remove the stale PROP_MGR/LANDLORD "Next steps" bullets only if they are done (else leave).
