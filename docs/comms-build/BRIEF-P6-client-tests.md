# Close-out / B: a client test harness and tests for the new UI (pane bottom-left)
Worktree: /Users/courtneebonnick/v6-wt-worker  (branch p6-client-tests)

CLOSE-OUT pass on the comms rebuild (after Phases 0–5 first pass). Read docs/comms-build/HANDOVER.md, docs/comms-build/CUTOVER.md and the "Not done" sections of docs/comms-build/P*-DONE.md in your worktree first; production is on this code with the spine in SHADOW, so nothing you build may change customer-facing behaviour unless it is behind an existing off-by-default switch. Rules: (1) work ONLY in your worktree; cd there for every command. (2) No dev server; no DB access except idempotent migration files (never db:push); never app_settings; no push. (3) VERIFICATION: zero NEW tsc errors vs your start commit (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; ~1,876 pre-existing; one tsc at a time), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x PHASE0_MERGED=1 npx vitest run`; 42 pre-existing), esbuild bundle of server/index.ts succeeds. (4) Commit on your branch. (5) Write `<worktree>/P6-DONE.md` (files, migrations, verification, not-done, decisions).

None of the new admin UI has been rendered by anyone (no client test setup exists; the panes could not open a browser). Build the harness and prove the components render and act.

## Build
1. Add a client vitest project: `vitest.config.ts` gains a second project (or `client/vitest.config.ts`) with `environment: 'jsdom'` (or happy-dom) and `@testing-library/react` + `@testing-library/jest-dom` as devDependencies (add to package.json; run `npm install` in the worktree — node_modules is a symlink to the main checkout, so install there is acceptable; note versions in DONE). Keep the server project untouched so the 42-baseline rule still holds; the client project must be green.
2. Tests (render + interaction with mocked fetch/SSE): `VerdictReasonChips` (five chips, tap submits reason), `DraftApprovalCard` flows in CommsPage (approve one-tap = reason fine; edit-then-approve shows chips; reject requires a reason; `DueChip` renders "due in"/"overdue"), `SampleReviewStrip` (lists sampler questions; fine/not fine posts the answer), `AgentRunsDrawer` (empty, not-switched-on, and 3 runs with expand), `QuoteIntakeCard` (missing-field chips, tick/untick media, save draft posts null prices), `SpineSwitchStrip` + `PackTiersBlock` on AgentStaffPage (mode pill, chips, ladder table), `WorkerHeartbeatStrip` (alive / stale / none).
3. Fix any real bug the tests expose in those components (keep fixes minimal and list them).
4. Add `npm run test:client` and mention it in docs/RUNBOOK.md §4 verification line.
