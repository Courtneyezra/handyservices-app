# Phase 5 / B: prepare the legacy retirement (pane bottom-left)
Worktree: /Users/courtneebonnick/v6-wt-worker  (branch p5-legacy-prep)

PHASE 5 of the comms rebuild ("Delete"), first pass = everything that is safe BEFORE the legacy agent is retired. Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md §10 (Phase 5 row) and §11, and docs/comms-build/PHASE5-DELETE.md (in the main checkout), then `git log --oneline -80` in your worktree. Non-negotiable rules: (1) work ONLY in your worktree path. (2) No dev server; no DB access; migration files only if the brief says so (never db:push); never app_settings; no push. (3) VERIFICATION: zero NEW tsc errors vs your start commit (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; ~1,882 pre-existing; one tsc at a time; deleting files may REMOVE errors, that is fine), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x PHASE0_MERGED=1 npx vitest run`; 42 pre-existing), esbuild bundle of server/index.ts succeeds. (4) The legacy comms agent (server/agents/comms.ts and its sweeps) MUST keep working: the spine is only in shadow mode in production. (5) Commit on your branch. (6) Write `<worktree>/PHASE5-DONE.md`.

The legacy agent is NOT deleted in this pass. Everything here makes the later deletion a pure file removal.

## Build
1. `scripts/_approver-backfill.ts` — dry-run by default: maps legacy `message_drafts.approved_by` strings (`comms_agent:autosend` → `agent.comms.autosend`, `comms_agent:sla_chase` → `agent.sla_chase`, `hours_gate:morning_release` → `rules.hours_gate`, `first_contact_ack:*` → `rules.first_contact`, `v2_pipeline:autosend` → `agent.comms.autosend` with a note, bare emails → `human:<email>`) and prints counts per mapping; `--apply` runs the UPDATEs in one transaction and writes a `system_events` row. Do not run it. Tests for the pure mapping.
2. Remove dead code that nothing needs today: the `resolve_question` tool in `server/agents/comms.ts`, the `autosend.intents` field (config type, defaults, the operator script flag), the `quotePriceSourceRefusal` comment block, and the duplicated `isTestNumber` in comms-lanes.ts / comms-sweep.ts (one export in rules-layer.ts or phone-utils). Keep `COMMS_CONFIG_OVERRIDE` (suites still use it).
3. Archive the legacy standing orders verbatim to `docs/archive/legacy-comms-prompt-2026-09.md` (the SYSTEM string and postQuoteStandingOrders) with a header noting the spine prompts that replaced them.
4. `docs/comms-build/PHASE5-LEGACY-RETIRE.md`: the exact file list, importers to update, config keys to drop, /admin/staff card to remove, and the architecture-test edits for the day the legacy agent is deleted; plus the go/no-go query set (spine live ≥ 7 days, zero unsafe verdicts, holding-line rate, Ben queue size).
