# Phase 0 / B: one worker, a heartbeat, no V2 (pane bottom-left)
Worktree: /Users/courtneebonnick/v6-wt-worker  (branch comms-v3/worker)

PHASE 0 of the comms rebuild ("Close the doors"). Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md sections 1.2, 3.1, 3.6, 3.9 and 10 (Phase 0 row) first. Non-negotiable working rules: (1) work ONLY inside your worktree path below; cd there for every command; never edit /Users/courtneebonnick/v6-switchboard itself or any other worktree. (2) Do not run the dev server, do not query or modify the database, do not touch app_settings, do not push. (3) node_modules is symlinked; run `npm run check` (tsc) and `npx vitest run` in your worktree and make both pass before you finish. (4) Commit on your branch with clear messages. (5) When finished write `<worktree>/PHASE0-DONE.md` containing: files changed, what you verified and how, anything you could not do and why, and any decision you made that the design did not specify. The orchestrator merges your branch; do not merge or rebase yourself.

## Build
1. **Worker gate.** New `server/worker-gate.ts`: `isCommsWorker()` = `process.env.COMMS_WORKER === '1'`; `describeWorkerState()`; `assertCommsWorkerAtBoot()` called once from server/index.ts after env load: if `NODE_ENV === 'production'` and not worker → console.error + Pushover alert "COMMS_WORKER flag absent on production: no sweeps will run" (find the existing Pushover helper in server/, grep -ri pushover server/*.ts); if NOT production and DATABASE_URL contains the production Neon host (`ep-broad-king`) → console.warn loudly that sweeps are disabled because a dev process is pointed at the production database. Never throw at boot.
2. **Gate every loop that can reach a customer.** `server/agents/comms-sweep.ts` (both setInterval sites ~467 and ~483 plus the boot catch-up), and in `server/cron.ts` every schedule that can send or draft to customers: lines ~52 (sweepCommsAgent), ~69 (windowClosingSweep), ~87 (backlogSweep), ~104, ~116, ~129 (check each: dunning, day-before reminders, customer notifications count as customer-facing). Leave SEO/GMB/GSC pulls ungated. Gated = the schedule is registered only when `isCommsWorker()`; log one line per skipped loop at boot.
3. **Heartbeat.** The worker writes `app_settings` key `comms_worker_heartbeat` = `{ at: ISO, pid, host, version: commit sha if available }` every 60 s from the fast tick. Add `GET /api/health/comms-worker` returning `{ ok, ageSeconds, stale: age > 600 }` for Railway/uptime checks. Add an in-process check every 5 min: if stale > 10 min during 08–20 Europe/London and we ARE the worker, Pushover once per hour "comms worker heartbeat stale". Use the existing app_settings helpers (grep getSetting/setSetting or the appSettings table in shared/schema.ts). Show `stale`/`at` on /api/agents/staff payload if there is a natural place (server/agent-staff.ts); optional.
4. **Delete the V2 path.** In `server/agents/comms-sweep.ts` remove `sendV2Reply`, the `shouldUseV2` imports and both `if (shouldUseV2(...))` branches so the sweep always calls `runCommsAgent`. Do NOT delete server/pipeline/v2.ts or server/workers (Phase 5). Do NOT rename approver strings (another pane owns that; if you must touch `approveAndSendDraft` calls, leave the second argument exactly as it is).
5. Write the ops checklist `docs/comms-build/PHASE0-OPS.md`: set `COMMS_WORKER=1` on Railway (service handyservices-app), create a Neon dev branch and point local .env at it, remove TWILIO_AUTH_TOKEN / WHATSAPP_ACCESS_TOKEN from local .env files, kill the six `tsx watch` dev processes on the Mac (pids visible via `ps aux | grep 'tsx watch'`), verify `/api/health/comms-worker` after deploy.

## Done when
tsc clean, vitest green, no `shouldUseV2`/`sendV2Reply` in server/agents, every customer-facing loop gated, PHASE0-DONE.md written.
