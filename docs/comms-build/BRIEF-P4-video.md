# Phase 4 / A: describe_video via Gemini, direct (pane top-right)
Worktree: /Users/courtneebonnick/v6-wt-exit  (branch p4-video)

PHASE 4 of the comms rebuild ("Widen"). Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md sections 3.4, 3.5, 3.8, 7 and 10 (Phase 4 row), then `git log --oneline -60` in your worktree and `server/spine/` (Phases 2–3). Non-negotiable rules: (1) work ONLY in your worktree path. (2) No dev server; no DB access except migration files (idempotent SQL + shared/schema.ts; never db:push); never app_settings; no push; no external API calls unless the brief says a script may with an explicit flag. (3) VERIFICATION: zero NEW tsc errors vs your start commit (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; one tsc at a time), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x npx vitest run`; 42 pre-existing), esbuild bundle of server/index.ts succeeds. (4) Ships dark behind `app_settings.spine` fields defaulting off. (5) Commit on your branch. (6) Write `<worktree>/PHASE4-DONE.md`.

## Build
1. `server/spine/tools/describe-video.ts`: `describeMedia({ path|url, kind: 'video'|'image' })` → fixed schema `{ whatIsShown: string; whatIsMissing: string[]; defects: { item: string; severity: 'minor'|'moderate'|'severe'; note: string }[]; textFound: string[]; confidence: 'low'|'medium'|'high' }`, validated with zod. Calls Gemini 2.5 Flash DIRECTLY over HTTPS (`GEMINI_API_KEY`, already on Railway; do NOT use OpenRouter or anything in server/llm/openrouter.ts). Native video bytes (inline base64 for ≤ 20 MB, else the Files API upload flow). Salvage the prompt and MediaExtraction shape from `server/workers/vision.ts` (read it; do not import it). Timeout 60 s, one retry, then `null` with a logged reason; never throws into the case file.
2. Cache results by media id + byte hash in `server/storage/media/.descriptions/<hash>.json` (gitignored) so a media item is described once, and store the description on the case-file media item (`MediaItem.description`) in `server/spine/case-file.ts` when `spine.video.enabled` (new config field, default false) is on. Images: reuse the existing image-block path (media-context.ts); only VIDEO goes to Gemini by default (`spine.video.images: false`).
3. Cost log: token/byte usage per call into agent_runs via a child run row (agent \x27triage\x27 or a new AgentName? add \x27vision\x27 to the AgentName union ADDITIVELY in types.ts and vocab.ts) with `cost_pence` using Gemini Flash pricing (look it up in the repo if present; else assume $0.10/M input tokens video, note it).
4. `scripts/_describe-media.ts <file>` for the owner to try on a local file; runs only with `--live`.
5. Tests: schema validation, cache hit/miss, null-on-failure with fakes. No network.
