# Phase 1 / C: verdict capture + agent runs drawer (pane bottom-right)
Worktree: /Users/courtneebonnick/v6-wt-config  (branch p1-verdicts, created by the orchestrator from comms-v3)

PHASE 1 of the comms rebuild ("See everything, never go silent"). Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md sections 2, 3.5, 3.7, 4, 8 and 10 (Phase 1 row) first, then `git log --oneline -15` in your worktree to see what Phase 0 added (server/approver.ts with the Approver union + newRunId, server/worker-gate.ts with isCommsWorker, the gated sendCustomerMessage). Non-negotiable working rules: (1) work ONLY inside your worktree path; cd there for every command; never edit the main checkout or another worktree. (2) Do not run the dev server; do not run queries against the database except through migration files you write (the orchestrator applies migrations); never touch app_settings; do not push. (3) Migrations: write a dated SQL file in migrations/ (idempotent: IF NOT EXISTS / ADD COLUMN IF NOT EXISTS) AND the matching Drizzle change in shared/schema.ts. Never run db:push. (4) node_modules is symlinked; `npm run check` and `npx vitest run` must pass. (5) Commit on your branch. (6) When finished write `<worktree>/PHASE1-DONE.md`: files changed, migrations to apply (exact filenames), how you verified, anything not done and why, decisions the design left open.

## Build
1. **Verdicts.** Migration + schema: `draft_verdicts(id uuid pk default gen_random_uuid(), draft_id uuid, run_id text null, verdict text check in ('approve','edit','reject'), reason text check in ('fine','tone','wrong_move','unsafe','missing_info') null, original_body text, final_body text null, by text, created_at timestamptz default now())` + index on (created_at). Server: in `server/message-drafts.ts` routes, approve/reject/patch write a verdict row (edit = PATCH then approve with body differing). Add `GET /api/verdicts/stats?days=30` returning per-source/per-intent counts, unedited-approval rate, reject reasons. Sample reviews (Phase 3) will reuse this table: allow verdict 'sample_fine' | 'sample_not_fine' too.
2. **UI in /admin/comms** (client/src/pages/admin/CommsPage.tsx, the pending-draft block around line 786 `act()`): approve stays one tap (reason defaults to 'fine'); edit-then-approve prompts for a reason chip; reject requires a reason chip. Chips: fine / tone / wrong move / unsafe / missing info. Show `due_at` on drafts and flags as "due in 2h" / "overdue" if the column exists on the payload (pane B adds it; render conditionally).
3. **Agent runs drawer.** `GET /api/agent-runs?conversationId=` (requireAdmin) reading `agent_runs` (pane A creates it; if the table is absent in your worktree, define the query against the schema you expect: id, agent, trigger, decision, lane, guards_hit, usage, cost_pence, started_at, finished_at, error). In ThreadPanel add a collapsible "What the agent did" drawer listing runs newest first with a one-line summary each and expandable details (proposal, guards hit, cost). Render an empty state when there are none.
4. **/admin/staff**: add worker heartbeat (from `/api/health/comms-worker`, Phase 0) and the 30-day verdict stats per agent to the staff cards (client/src/pages/admin/AgentStaffPage.tsx, server/agent-staff.ts).
5. **Tests.** vitest for the verdict stats aggregation with a fake dataset. `npm run check` clean (client + server).

## Done when
Every approve/edit/reject writes a verdict with a reason, stats endpoint works, drawer renders runs, staff page shows heartbeat and verdict rate, migration listed.
