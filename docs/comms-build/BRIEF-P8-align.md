# P8 / C: one intake, one vocabulary, one card (pane bottom-right)
Worktree: /Users/courtneebonnick/v6-wt-config  (branch p8-align)

P8 — automate the last step: intake → estimate → priced draft → Ben confirms → send ("Route A"). Decisions locked by the owner 3 Sep: (a) the estimator judges (category, time as a range, materials with cost, access/difficulty flags, confidence) and NEVER outputs a price; (b) the ONLY pricing engine is server/contextual-pricing/multi-line-engine.ts with the live settings row (materialsMarginPercent = 27 today; never hardcode); the additive module in server/pricing-config.ts and the `/api/quotes/from-estimate` route are retired; (c) labour = estimator on-site minutes per line + ONE setup and ONE cleanup allowance per job (no per-line buffers); (d) fallback when a line cannot be estimated = reference-rate price with a `check_this` flag and the reason; (e) `visit_first` readiness → a DRAFT survey offer for Ben to approve (fixed fee from settings, booking link); (f) both legacy paths go NOW: `maybeAutoQuotePrep` in server/agents/comms.ts stops firing (the spine clerk is the only intake, it already runs in shadow and its card reads regardless of mode), and `from-estimate` is deleted; (g) Ben prices on a new phone-first "price and send" screen; the contextual generator stays one tap away. Money rule unchanged: no price reaches a customer without Ben's tap. Rules: work only in your worktree; no dev server; no DB access except idempotent migration files (never db:push); never app_settings; no push; verification = zero new tsc errors vs your start commit (~1,872 pre-existing), server vitest failures identical to baseline (42, `DATABASE_URL=postgres://u:p@127.0.0.1:1/x PHASE0_MERGED=1 npx vitest run --project server`), `npm run test:client` green, esbuild bundle OK; commit on your branch; write `<worktree>/P8-DONE.md`. Read docs/COMMS_AGENTS_V3_DESIGN.md §6, the Obsidian-derived findings in docs/comms-build/HANDOVER.md, and the code map below before starting.

Code map (from the 3 Sep audit): legacy intake = comms.ts `maybeAutoQuotePrep` (:1558) → `runQuotePrep` → `conversations.metadata.quotePrepIntake` (+ `quotePrepAuto`), board pill "Intake ready to price" (inbox-board.ts:424, CommsPage.tsx:570), QuotePrepPanel + header "Build Quote" (CommsPage.tsx:1494,1637,1655), portal lane override (portal-routes.ts:39-91). Spine intake = server/spine/agents/quote-clerk.ts wrapping runQuotePrep → Proposal.artifact quote_intake on agent_runs → GET /api/spine/quote-intake/:id → QuoteIntakeCard (CommsPage.tsx:1652) → save-draft (server/spine/quote-intake.ts:180-215, personalized_quotes.is_draft, prices null). Estimator = server/agents/quote-estimator.ts `runEstimator` (+ estimator-tools.ts: search_materials, get_time_history, get_procedures, submit_build), entry POST /api/pricing/estimate-build (estimate-routes.ts:122, in-memory jobs 10-min TTL, short-circuits to a `quote_research` row when one exists :136-153), output QuoteBuild (shared/quote-build.ts), consumed by QuoteBuilderPanel / GenerateContextualQuote `hydrateFromEstimatorBuild`, and by `/api/quotes/from-estimate` (quotes.ts:3293, prices with the stale additive engine :3380 and sends). Readiness vocab: quote-prep 5 values, board 4, portal 3.

## Build
1. **One readiness vocabulary.** `shared/intake-readiness.ts`: `quote_ready | quote_pending | needs_info | visit_first | decline` with labels and colours; portal-routes.ts LANES, inbox-board.ts, CommsPage chips, PortalReviewPage action bar and the spine clerk all import it. `decline` gets a lane and a portal control (Ben confirms a decline; the spine drafts the polite decline reply at DRAFT tier — intent `closing` already exists).
2. **One intake source.** `server/intake.ts` `getIntake(conversationId)` returns the newest spine clerk artifact (agent_runs) as the intake; legacy `metadata.quotePrepIntake` is read ONLY as a fallback for threads that predate the spine and is never written again. inbox-board.ts `intakeReadiness`, `GET /api/agents/quote-prep/:id/intake`, portal-routes lane override (now writes a `quote_intake_override` on the thread metadata that getIntake applies on top of the spine artifact), and PortalReviewPage all go through getIntake. The manual re-run button (`POST /api/agents/quote-prep/:id`) now calls `requestRun(conversationId, 'manual')` so the spine clerk runs, instead of legacy runQuotePrep.
3. **One card in the thread.** Remove the legacy QuotePrepPanel slide-over, its chip and the header "Build Quote" from CommsPage.tsx; QuoteIntakeCard becomes the single entry: readiness pill (shared vocabulary), lines, gaps, media ticks, and a "Price and send" button once a priced draft exists (link to `/admin/price/<slug>`, pane B) or "Estimating…" while the chain runs (read estimate status from pane A's table via the intake route; if absent, show the intake only). Portal page uses the same card component.
4. **Research: one system.** `quote_research` (heuristic) stops being auto-queued (comms.ts:1721, portal-routes.ts:80); keep its table and `processResearchJob` only as the explicit fallback the estimator uses when it has no history and no catalogue match (pane A's `timeSource:'fallback'`); estimate-routes.ts no longer short-circuits to it silently (:136-153) — either return the estimator's own result or a clearly labelled fallback.
5. **Docs**: design §6 addendum "Route A (built 3 Sep)" with the contract "clerk scopes, estimator measures, engine prices, Ben decides", the fallback rule, the visit-first rule, and the Route B graduation trigger unchanged; HANDOVER.md: Ben's new price-and-send flow; CUTOVER.md: note the legacy quote-prep is already off.
6. Eval cases: `eval-cases/intake/` — scope added after intake → new intake supersedes (uses the Gemma window-sill example, scrubbed), visit_first → offer_survey draft, decline → closing draft; harness family wiring as in Phase 3.
7. Tests for the shared vocabulary, getIntake precedence (spine > override > legacy fallback), and the card states (client).
