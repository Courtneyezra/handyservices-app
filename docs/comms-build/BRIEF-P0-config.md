# Phase 0 / C: process-local config + architecture tests (pane bottom-right)
Worktree: /Users/courtneebonnick/v6-wt-config  (branch comms-v3/config)

PHASE 0 of the comms rebuild ("Close the doors"). Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md sections 1.2, 3.1, 3.6, 3.9 and 10 (Phase 0 row) first. Non-negotiable working rules: (1) work ONLY inside your worktree path below; cd there for every command; never edit /Users/courtneebonnick/v6-switchboard itself or any other worktree. (2) Do not run the dev server, do not query or modify the database, do not touch app_settings, do not push. (3) node_modules is symlinked; run `npm run check` (tsc) and `npx vitest run` in your worktree and make both pass before you finish. (4) Commit on your branch with clear messages. (5) When finished write `<worktree>/PHASE0-DONE.md` containing: files changed, what you verified and how, anything you could not do and why, and any decision you made that the design did not specify. The orchestrator merges your branch; do not merge or rebase yourself.

## Build
1. **Port process-local comms config.** The worktree `/Users/courtneebonnick/v6-switchboard/.claude/worktrees/angry-merkle-e4798d` (branch `claude/dreamy-lamarr-4da59b`) contains `useProcessLocalCommsConfig` in `server/agents/comms.ts` plus its use in test suites (scripts/_*.ts). Read `git -C /Users/courtneebonnick/v6-switchboard diff main..claude/dreamy-lamarr-4da59b -- server/agents/comms.ts` and port ONLY the process-local config mechanism (the override store, `useProcessLocalCommsConfig`, and `getCommsAgentConfig`/`setCommsAgentConfig` honouring it) into your worktree's `server/agents/comms.ts`. Do not bring any other diff from that branch (it is 97 commits behind). Then make every `scripts/_*.ts` suite that flips comms config (grep -l setCommsAgentConfig scripts) call `useProcessLocalCommsConfig()` first so suites can never flip the shared production row again (the 21 Aug incident).
2. **Architecture tests** in `server/__tests__/architecture.test.ts` (vitest, pure file reads with fs + a small TS-agnostic scan; no DB). Tests: (a) `shouldUseV2` and `sendV2Reply` do not appear anywhere under server/agents; (b) the set of files importing `approveAndSendDraft` is exactly the allowed list [server/agents/comms.ts, server/agents/comms-sweep.ts, server/agents/sla-sweep.ts, server/agents/promise-tracker.ts, server/auto-ack-window.ts, server/first-contact-ack.ts, server/outbound.ts, server/agent-staff.ts]; (c) the Twilio/Meta send primitives (find them: grep for `.messages.create(` and the Meta Graph send in server/) are called only from server/outbound.ts, server/whatsapp-api.ts and server/sms.ts, nothing else; (d) `server/agents/comms-sweep.ts` and `server/cron.ts` both import from `../worker-gate` / `./worker-gate`; (e) no file under server/ contains the literal regex `AUTOMATED_APPROVER`. Tests (a), (d), (e) and part of (b)/(c) depend on the other two panes' branches, which are NOT in your worktree: wrap those with `describe.skipIf(!process.env.PHASE0_MERGED)` so they run green today and are enabled after merge. Keep (c) live now if it already holds.
3. **Prod-DB test guard.** Add `server/__tests__/setup.ts` (register in vitest config) that throws if `DATABASE_URL` contains `ep-broad-king` and `ALLOW_PROD_DB_TESTS` is not set, so vitest can never run against production by accident.
4. Make `npm run check` and `npx vitest run` pass in your worktree.

## Done when
process-local config ported and used by every config-flipping suite, architecture tests present and green (skipped parts labelled), prod-DB test guard active, PHASE0-DONE.md written.
