# Phase 4 / C: contractor pack + post-call merge gate (pane bottom-right)
Worktree: /Users/courtneebonnick/v6-wt-config  (branch p4-contractor)

PHASE 4 of the comms rebuild ("Widen"). Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md sections 3.4, 3.5, 3.8, 7 and 10 (Phase 4 row), then `git log --oneline -60` in your worktree and `server/spine/` (Phases 2–3). Non-negotiable rules: (1) work ONLY in your worktree path. (2) No dev server; no DB access except migration files (idempotent SQL + shared/schema.ts; never db:push); never app_settings; no push; no external API calls unless the brief says a script may with an explicit flag. (3) VERIFICATION: zero NEW tsc errors vs your start commit (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; one tsc at a time), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x npx vitest run`; 42 pre-existing), esbuild bundle of server/index.ts succeeds. (4) Ships dark behind `app_settings.spine` fields defaulting off. (5) Commit on your branch. (6) Write `<worktree>/PHASE4-DONE.md`.

## Build
1. **Contractor liaison agent** `server/spine/agents/contractor-liaison.ts`: SpineAgent \x27contractor_liaison\x27 (tier DRAFT) on the runner (Sonnet 5), pack `contractor.default` (intents job_brief, availability_ask, confirm_receipt, materials_list; guards customer_pii, money_to_customer). Implement the two contractor guards in `server/spine/guards.ts` if missing: `customer_pii` (customer full name + address/postcode + phone in one message to a contractor is banned; postcode-only allowed), `money_to_customer` (n/a for contractor audience; keep as no-op with a comment). Case file for a contractor thread: the job(s) they are assigned (job-assignment / v2Bookings tables — find them), the brief, materials list if any, and the thread. `accepts` = triage.audience === \x27contractor\x27. Triage: audience from `server/roles.ts` resolver (conversations.role_profile). Register in agents/index.ts.
2. **Post-call merge gate**: the worktree `remove-realtime` (branch, find with `git branch -a | grep -i realtime`; commit 72c1bff) removed the realtime media stream and moved call finalize to recording-status. Do NOT merge it blindly: write `server/__tests__/post-call-ladder.test.ts` that exercises, with fakes, the four call types the design names (answered / missed-after-ring / abandoned-mid-ring / outbound) against the CURRENT code in server/call-thread.ts + first-contact-ack.ts + call-lead.ts: asserting (a) missed → one text-back within the ladder, (b) answered → no text-back, transcript → triage/clerk hook, (c) abandoned mid-ring → ack only if the abandon is fresh, (d) outbound → recorded, no customer ack. Report which pass today and which need the remove-realtime work; do not port that branch in this phase unless a test fails for a reason its diff clearly fixes and the fix is < 50 lines.
3. **Contractor comms card**: `/admin/contractors/:id` already has a CommsCard per memory; make sure a contractor thread shows the pack chip and the liaison\x27s drafts land in the same approval UI (source \x27spine\x27, audience contractor) — small client change in CommsPage to show an "audience: contractor" chip on the thread header.
4. Data precondition note in DONE: the lane cannot light up until real phones exist on the 8 contractor users (empty strings today).
5. Tests: guards for customer_pii cases; liaison belt with a stubbed runner (3 cases).
