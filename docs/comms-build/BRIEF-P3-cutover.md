# Phase 3 / C: eval families, holding template, cutover checklist (pane bottom-right)
Worktree: /Users/courtneebonnick/v6-wt-config  (branch p3-cutover)

PHASE 3 of the comms rebuild ("Earn sending"). Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md sections 4, 5, 9 and 10 (Phase 3 row) and section 0b (fast track), then `git log --oneline -40` in your worktree and `server/spine/` (Phase 2: request-run, case-file, triage, packs, guards, decide, exit, agents/scoper, agents/quote-clerk, agents/recovery, the eval harness scripts/eval-comms.ts and eval-cases/). Non-negotiable rules: (1) work ONLY in your worktree path. (2) No dev server; no DB access except migration files (idempotent SQL + shared/schema.ts; never db:push); never app_settings; no push. (3) VERIFICATION: zero NEW tsc errors vs your start commit (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; one tsc at a time), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x npx vitest run`; 42 pre-existing), esbuild bundle of server/index.ts succeeds. (4) Nothing you build changes production behaviour by itself: promotion/demotion and the sampler ship behind `app_settings` key `spine` fields (`enabled`, `autonomy.enabled`, `sampler.enabled`) that default false, fail closed. (5) Commit on your branch. (6) Write `<worktree>/PHASE3-DONE.md` (files, migrations, verification, not-done, decisions).

## Build
1. **Eval families per intent** for every intent in `customer.default` and `customer.post_quote` (ask_gap, clarify_scope, confirm_received, faq_from_kb, point_to_quote_page, closing, answer_from_quote, point_to_picker): ≥ 5 cases each under `eval-cases/<intent>/`, built from the seed corpus contexts (eval-cases/seed/real-sends.json) and the replay examples in docs/comms-model/replay-summary.md, with expected lane/intent, mustNotContain (money, dates, discounts), mustFlag where applicable, and absence cases (must NOT escalate). Make `scripts/eval-comms.ts` report pass^3 per family and write the scoreboard pane A's autonomy job reads (agree the JSON shape by reading pane A's brief BRIEF-P3-autonomy.md §2: `{ families: { [name]: { passK: number, k: 3, passed: bool, at } } }` at `eval-results/latest.json`).
2. **Judge rubric `voice-v1`** in `server/spine/judge.ts` (Opus 5 via server/llm.ts, schema-validated): scores a proposal body against brand-voice/whatsapp-comms.md + shared/chat-voice.ts rules; advisory only; harness runs it when `EVAL_LIVE=1`. Store agreement with Ben's verdicts in a small report script `scripts/_judge-agreement.ts` (reads draft_verdicts; needs DB so it just prints the SQL plan and runs only with `ALLOW_PROD_DB_TESTS=1`).
3. **Meta template for the holding line**: write the template submission as code, not by hand: `scripts/_submit-holding-template.ts` using the existing template sync module (server/whatsapp-template-sync.ts) or Twilio Content API client already in the repo, name `holding_line_v1`, body "Hi {{1}}, thanks for your message, we've got it. Just looking at it now, we'll come back to you shortly." (utility category, en_GB). Do NOT run it (external submission is the owner's call); make it print exactly what it will submit and exit unless `--submit`. Wire `HOLDING_TEMPLATE_PREFERENCE` in server/rules-layer.ts to the name.
4. **Wire ask_media / ask_postcode**: the rules layer has `sendAsk` with no caller. Call it from the spine exit when triage says first contact and the case file has no media (ask_media) or no postcode in the last 30 days of timeline (ask_postcode), max one ask per thread per 24h, never both in one tick, suppressed by the same rules as the holding line. Behind spine mode live/shadow (shadow = log only).
5. **Cutover checklist** `docs/comms-build/CUTOVER.md`: exact steps for the orchestrator to go off → shadow → live: flags to flip, what to watch (health, agent_runs decisions, holding-line rate, Ben's queue size, opt-outs), rollback in one command, and the kill criteria from design §10.
6. Tests: family JSON schema validation; judge schema parse; ask wiring decision function with fakes.
