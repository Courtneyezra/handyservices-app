# Phase 3 / A: promotion, demotion, fast track (pane top-right)
Worktree: /Users/courtneebonnick/v6-wt-exit  (branch p3-autonomy)

PHASE 3 of the comms rebuild ("Earn sending"). Read /Users/courtneebonnick/v6-switchboard/docs/COMMS_AGENTS_V3_DESIGN.md sections 4, 5, 9 and 10 (Phase 3 row) and section 0b (fast track), then `git log --oneline -40` in your worktree and `server/spine/` (Phase 2: request-run, case-file, triage, packs, guards, decide, exit, agents/scoper, agents/quote-clerk, agents/recovery, the eval harness scripts/eval-comms.ts and eval-cases/). Non-negotiable rules: (1) work ONLY in your worktree path. (2) No dev server; no DB access except migration files (idempotent SQL + shared/schema.ts; never db:push); never app_settings; no push. (3) VERIFICATION: zero NEW tsc errors vs your start commit (`NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit -p tsconfig.json 2>&1 | sort`, diff ignoring line numbers; one tsc at a time), vitest failures identical to baseline (`DATABASE_URL=postgres://u:p@127.0.0.1:1/x npx vitest run`; 42 pre-existing), esbuild bundle of server/index.ts succeeds. (4) Nothing you build changes production behaviour by itself: promotion/demotion and the sampler ship behind `app_settings` key `spine` fields (`enabled`, `autonomy.enabled`, `sampler.enabled`) that default false, fail closed. (5) Commit on your branch. (6) Write `<worktree>/PHASE3-DONE.md` (files, migrations, verification, not-done, decisions).

## Build
1. **Pack tier state in the DB.** Migration + schema: `pack_intent_tiers(pack_id text, intent text, tier text, reason text, changed_by text, changed_at timestamptz default now(), primary key (pack_id, intent))` + `pack_tier_events` append-only log (id, pack_id, intent, from_tier, to_tier, reason, evidence jsonb, by, at). `server/spine/packs.ts` `resolvePack` overlays DB tiers on the static config (static = launch defaults; DB = earned state). Never write SEND for an intent that is not in the pack's allowedIntents, and never for money/date intents (they do not exist; assert).
2. **Promotion job** `server/spine/autonomy.ts` `evaluateAutonomy({dryRun})`: for each (pack, intent) at DRAFT compute the §4 evidence: eval family status from the latest `eval-results/*.json` scoreboard (family `<intent>` pass^3 = 100%), verdicts across the pack in 30 days (draft_verdicts joined to message_drafts.run_id → agent_runs.pack_id + proposal.intent; ≥ 30, unedited ≥ 90%), zero `unsafe` on the intent ever, zero guard escalations attributed to the intent in 14 days (agent_runs.guards_hit + decision). **Fast track (§0b):** `ask_gap` and `confirm_received` may promote without the eval-family precondition after ≥ 14 days AND ≥ 20 verdicts AND zero rejects AND unedited ≥ 90%. Promote = write pack_intent_tiers + event + Pushover to Courtnee (existing helper; event key `escalation` or a new `autonomy` key). Demotion (same job, every run): any `unsafe` verdict or sample `sample_not_fine` with reason unsafe on the intent in 30 days, any incident tag on a run of that intent, or trailing-30 sampled approval < 80% → DRAFT + event + Pushover. Idempotent, dry-run prints the table.
3. **Cron** (worker-gated, behind `spine.autonomy.enabled`): daily 07:30 Europe/London. Also `scripts/_autonomy-report.ts --dry-run` for the owner.
4. **Staff page**: extend `/api/agents/staff` with per-pack intent tiers + last change + the evidence numbers; render in AgentStaffPage dossier as a small table (intent · tier · verdicts/30d · unedited % · unsafe · eval family · last change).
5. Tests: vitest for the promotion/demotion decision function over fake evidence (all branches incl. fast track and the money/date assertion).
