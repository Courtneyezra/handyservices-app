# Close-out / A2: switch controls, template status and shadow panel on /admin/staff (pane top-right, after BRIEF-P6-seams)
Worktree: /Users/courtneebonnick/v6-wt-exit  (same branch p6-seams)

Same rules as BRIEF-P6-seams.md. Design §3.9 says every kill switch is "visible and flippable on /admin/staff, every flip logged"; today the switch strip is display-only, so flips need a script. CUTOVER §0 also says templates can be checked on /admin/staff, which is not true yet. Close both.

## Build
1. **Switch controls.** `POST /api/spine/config` (requireAdmin) accepting a partial `{ mode?, agents?, asks?, autonomy?, sampler?, video? }` → `setSpineConfig(patch, 'human:<id>')` (it already logs a `config_change` system event); refuse `mode:'live'` unless the body carries `confirm: 'LIVE'`. `POST /api/comms-agent/config` for the two legacy flags (`autosend.enabled`, `onInbound`) via `setCommsAgentConfig`, same confirm rule for turning autosend ON. Client: in `SpineSwitchStrip` each chip becomes a toggle (optimistic, refetch on settle); the mode pill opens a 3-way segmented control off / shadow / live with a typed confirmation for live and a one-line "what happens" caption per mode (from CUTOVER §2–3); a "Rollback to off" button always visible. Every control shows who last changed it and when (from system_events, latest `config_change` with source spine / comms).
2. **Template status.** `GET /api/whatsapp-templates/status` (requireAdmin) returning the cached approved-template list from the sync module (name, status, language, category, lastSyncedAt) plus the names the rules layer and packs expect (`holding_line_v1`, `missed_call_ack`, video/postcode/call requests) marked present/approved/missing. A compact table on /admin/staff under the switch strip with a "Sync now" button calling the existing sync function (worker-gated is not needed for a read; sync is a Twilio read).
3. **Shadow panel.** `GET /api/spine/shadow-report?days=1|7` running `compareShadow()` (Phase 3) and a card on /admin/staff: pairs, decision / intent / guard agreement, the decision matrix, last 10 pairs with thread links into /admin/comms. This is the go-live evidence Courtnee reads before flipping.
4. Tests: route input validation (confirm rule, unknown fields rejected), template status shaping, shadow report payload shape; client tests if the client harness from pane B has landed in your worktree (it may not; note it).
