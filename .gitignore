# Environment files (NEVER commit these!)
.env
.env.*

# WhatsApp Web session data
.wwebjs_auth/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
storage/recordings/*.raw
sqlite.db
*.log

# LLM Council reports / transcripts (local strategy artifacts)
council/

# WhatsApp chat exports (customer PII — never commit)
whatsapp-export/

# WhatsApp Web.js media cache (regenerated at runtime)
.wwebjs_cache/

# WhatsApp triage / cross-ref reports (customer PII — never commit)
WhatsApp_*.md
WhatsApp_*.docx
whatsapp-triage-report-*.md

# Uploaded files & contractor media (customer/contractor PII — never commit)
uploads/
server/storage/media/

# Ops scripts that read/push credentials (never commit)
*-creds.sh
