/**
 * OpsDock — the Ops Manager chat dock (fixed right panel + floating launcher).
 *
 * B-Phase0 stub: renders nothing so SidebarLayout can mount it now. B-WP3
 * replaces this with the real dock (contracts in shared/ops-types.ts).
 */
export default function OpsDock() {
    return null;
}
